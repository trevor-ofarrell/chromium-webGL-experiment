# Optimization Log

| ID | Optimization | Status | Mode | Evidence | Risk | Relevant files | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| O-001 | Use `content_shell` as minimal baseline target instead of Chrome browser UI | Drafted | Default | Chromium checkout is synced; `gn gen out\ReleaseBaseline` succeeds; `content_shell` build is blocked only by missing ATL/MFC host component; draft entrypoint patch applies cleanly with `git apply --check` | Medium: content shell differs from Chrome startup and embedder behavior | `content/shell/*`, `chromium_patches/0001-draft-minimal-three-viewer-entrypoint.patch` | Expected to reduce startup/memory more than steady-state FPS. Keep stock checkout pristine until baseline binary is built. |
| O-002 | Disable extensions/sync/autofill/translate/spellcheck/profile product services | Harness smoke only | Default where supported | Installed-Chrome smoke runner launches with `--disable-extensions`, `--disable-sync`, `--disable-background-networking`, and related flags; no same-revision fork measurement yet | Low to medium depending on hidden dependencies | `scripts/run_benchmark.mjs` | Many are absent from content shell already; verify with content-shell baseline after ATL is installed. |
| O-003 | Aggressive trusted-content GPU/runtime flags | Drafted | Trusted only | Runtime switch aliases are in the draft patch; no binary or benchmark yet | High: can reduce validation/security guarantees | `content/shell/app/shell_main_delegate.cc`, `content/shell/common/shell_switches.h` | Must be individually gated and benchmarked. |
| O-004 | Measure JS update time, render submission time, process-tree RSS, executable size, viewer bundle size, and optional package size | Kept | Benchmark harness | Current-schema benchmark JSON validates required fields plus value sanity through `scripts/validate_metrics.mjs` | Low: measurement-only change | `scripts/run_benchmark.mjs`, `scripts/validate_metrics.mjs`, `scripts/summarize_results.mjs` | Needed before any optimization claim can be accepted. |
| O-005 | Add runtime smoke and long-stability automation | Kept | Test harness | `smoke-installed-chrome-v11-stability-smoke.json` passes with launch, WebGL2, web-platform basics, basic input, WebGPU, texture, shader, and graphics-loss coverage; `smoke-installed-chrome-v6-short-stability-instancing-webgl2.json` validates the long-run wrapper | Low: test-only change | `scripts/run_smoke_tests.mjs`, `scripts/run_long_stability.ps1` | Short installed-Chrome stability validates the wrapper only; the one-hour fork gate remains pending. |
| O-006 | Add viewer navigation-lock automation | Pending fork binary | Script parses and is documented; no runtime result yet; HTTP navigation smoke now includes same-origin, loopback cross-origin blocking, deterministic external HTTP blocking, and `window.open` blocking | Low: test-only change | `scripts/run_navigation_lock_tests.mjs`, `chromium_patches/0001-draft-minimal-three-viewer-entrypoint.patch` | Requires fork build because stock Chrome ignores the viewer-specific switches. |
| O-007 | Add trusted-content runtime switch aliases | Drafted | Trusted viewer mode only | Patch applies cleanly; no binary yet | High: process isolation and GPU fallback behavior can change | `content/shell/app/shell_main_delegate.cc`, `content/shell/common/shell_switches.h`, `docs/trusted_content_flags.md` | Aliases implemented only when both `--viewer-app-url` and `--viewer-trusted-content` are present. Relaxed WebGL validation now maps to the pass-through command decoder; Blink feature disabling and direct presentation remain explicit no-op gates pending source experiments. |
| O-008 | Add official same-revision comparison workflow | Pending binaries | `run_official_comparison.ps1 -DryRun` emits expected commands; full run blocked by missing ATL/MFC build prerequisite | Low: orchestration-only change | `scripts/run_official_comparison.ps1`, `scripts/compare_results.mjs` | Keeps stock, fork default, and trusted/aggressive measurements consistent once binaries exist. |
| O-009 | Add viewer package staging | Kept for workflow; real package pending fork binary | `test_stage_viewer_package.ps1` passes with synthetic Chromium output and viewer dist, verifying runtime asset copies, selected runtime directories, viewer bundle copy, `-Clean`, package size output, and launcher switches | Low: packaging-only change | `scripts/stage_viewer_package.ps1`, `scripts/test_stage_viewer_package.ps1` | Provides package size input for benchmark JSON and a `run_viewer.ps1` launcher in the staged directory. Real package launch still requires the fork `content_shell.exe`. |
| O-010 | Add trace capture and coarse trace summary tooling | Kept for harness | Installed-Chrome trace smoke wrote `smoke-installed-chrome-v9-many-draw-calls-webgl2-trace.json` and summary report | Low: tracing can perturb timings, so traces are diagnostic rather than benchmark scores | `scripts/run_trace_capture.mjs`, `scripts/summarize_trace.mjs` | Same-revision stock/fork traces still pending. |
| O-011 | Add explicit viewer resource warmup experiment controls | Kept for harness | `smoke-installed-chrome-v10-resource-warmup-shader-heavy-webgl2.json` validates and records precompile/prerender timing fields | Low when opt-in; can hide startup hitches if compared against default runs incorrectly | `viewer/src/main.js`, `viewer/src/metrics.js`, `scripts/run_benchmark.mjs` | Disabled by default. Claims require paired default-vs-warmup measurements. |
| O-012 | Add graphics-loss and render-error stability instrumentation | Kept for harness | `smoke-installed-chrome-v11-stability-smoke.json` passes WebGL context-loss and WebGPU device-loss signal tests; `smoke-installed-chrome-v11-stability-fields-many-draw-calls-webgl2.json` records zero loss events and zero render errors | Low: diagnostic-only fields outside required schema | `viewer/src/main.js`, `scripts/run_smoke_tests.mjs`, `docs/stability_behavior.md` | Fork behavior still must be validated with the one-hour stock/fork stability gate. |
| O-013 | Add fork-only trusted experiment matrix runner | Pending fork binary | Script parses and can dry-run selected experiments; no fork measurements yet | Low for orchestration, high for any unsafe runtime flags it invokes | `scripts/run_trusted_experiment_matrix.ps1`, `docs/benchmark_methodology.md` | Runs risky flags one at a time so effects are not hidden inside a combined aggressive profile. |
| O-014 | Add file-mode package benchmark support and trusted file-access alias | Pending fork binary | Installed-Chrome v13 file-mode glTF smoke passes with `--allow-file-access-from-files`; draft patch aliases the same Chromium switch only for trusted local file viewer launches | High if used outside trusted local package mode | `scripts/run_benchmark.mjs`, `viewer/vite.config.js`, `chromium_patches/0001-draft-minimal-three-viewer-entrypoint.patch`, `docs/trusted_content_flags.md` | Required for packaged `file://` launch because stock Chrome does not complete the glTF file-mode smoke without file-access relaxation. |
| O-015 | Add WebGPU GPU frame timing via timestamp queries | Kept for harness | Three.js WebGPU renderer now requests timestamp tracking, and `smoke-installed-chrome-v14-webgpu-timestamp-instancing-webgpu.json` plus the v17 WebGPU suite record populated `avg_gpu_frame_ms` values | Low to medium: timing queries add measurement overhead and may be unavailable or gated by browser/GPU feature exposure | `viewer/src/renderers.js`, `viewer/src/webgpu_gpu_timer.js`, `viewer/src/main.js`, `scripts/run_benchmark.mjs` | `--disableGpuTiming` exists for instrumentation-overhead experiments. Same-revision stock/fork WebGPU measurements are still pending the Chromium build. |
| O-016 | Replace WebGPU postprocessing fallback with render-target/full-screen pass | Kept for harness, official evidence pending | `smoke-installed-chrome-v15-webgpu-postprocessing-render-target-webgpu.json` and the v17 WebGPU suite validate a WebGPU `RenderTarget` plus full-screen pass and record GPU timestamp data | Low: viewer-only benchmark scene change; still not exact shader parity with the WebGL custom blur/scanline pass | `viewer/src/scenes.js`, `docs/webgpu_scene_coverage.md` | This moves the scene from fallback to partial parity. A custom WGSL/TSL effect shader remains future work. |
| O-017 | Replace WebGPU shader-heavy fallback with WGSL node material | Kept for harness, official evidence pending | `smoke-installed-chrome-v16-webgpu-shader-heavy-wgsl-webgpu.json` and the v17 WebGPU suite validate a Three.js WebGPU node material with a WGSL loop matching the WebGL scene loop count | Low: benchmark-scene-only change; WGSL and GLSL source are similar but not byte-identical | `viewer/src/scenes.js`, `docs/webgpu_scene_coverage.md` | Removes the old `MeshPhysicalMaterial` fallback so WebGPU shader-heavy is now a real shader math stress path. |
| O-018 | Add renderer resource growth counters | Kept for harness, official evidence pending | `smoke-installed-chrome-v18-renderer-resource-counters-instancing-webgl2.json` and `...webgpu.json` validate geometry, texture, and program start/end/peak/delta fields with zero measured deltas | Low: diagnostic-only benchmark fields | `viewer/src/metrics.js`, `scripts/validate_metrics.mjs`, `docs/stability_behavior.md` | Supports the completion gate for no unbounded shader/program/texture growth after warmup; one-hour stock/fork validation remains pending. |
| O-019 | Enforce strict inputs for official comparison reports | Kept for workflow | `compare_results.mjs --strictOfficial` rejects installed-browser smoke inputs and requires checkout-built inputs, one Chromium revision, build-args hashes, and baseline/fork cases; `run_official_comparison.ps1 -DryRun` shows both official reports use it | Low: validation-only change | `scripts/compare_results.mjs`, `scripts/run_official_comparison.ps1`, `docs/benchmark_methodology.md` | Prevents accidental performance claims from mismatched revisions or smoke artifacts. |
| O-020 | Validate runtime and navigation smoke result semantics | Kept for workflow | `validate_smoke_result.mjs --type runtime` validates the installed-Chrome v11 smoke result and audit rows now require smoke validation rather than JSON parsing only | Low: validation-only change | `scripts/validate_smoke_result.mjs`, `scripts/audit_artifacts.ps1`, `scripts/verify_prebuild.ps1` | Fork runtime and navigation smoke artifacts remain pending until binaries exist. |
| O-021 | Validate complete benchmark scene suites before official reporting | Kept for workflow | `validate_benchmark_suite.mjs` accepts the installed-Chrome v17 WebGPU harness suite, rejects it when `--requireCheckout` is enabled, and `run_official_comparison.ps1 -DryRun` now shows suite validation gates before reports | Low: validation-only change | `scripts/validate_benchmark_suite.mjs`, `scripts/run_official_comparison.ps1`, `scripts/audit_artifacts.ps1`, `docs/benchmark_methodology.md` | Official performance rows now require validated complete suites with matching scene, renderer, variant, checkout, build-args, and exact expected fork-revision metadata rather than only glob counts. |
| O-022 | Make prebuild verification tolerate clean and already-patched Chromium checkouts | Kept for workflow | `verify_prebuild.ps1 -AllowMissingAtl` passes and now reports that the viewer patch applies cleanly; the same check will also accept the reverse-apply state after `build_viewer_fork.ps1 -ApplyPatch` | Low: validation-only change | `scripts/verify_prebuild.ps1`, `scripts/build_viewer_fork.ps1`, `docs/build.md` | Prevents the post-ATL workflow from treating an already-applied fork patch as a verifier failure during later fork iterations. |
| O-023 | Map Chromium source files for post-build WebGL/WebGPU/GPU/compositor optimization work | Kept for investigation, not performance evidence | `docs/source_investigation.md` records concrete files and classes found in the pinned checkout for content shell entrypoint, Blink WebGL, WebGPU/Dawn, canvas/ImageBitmap, Viz presentation, GPU process, and content-shell service removal candidates | Low: documentation-only; risk is using it as a substitute for measured changes, which is explicitly disallowed | `docs/source_investigation.md`, `src/content/shell/*`, `src/third_party/blink/renderer/modules/webgl/*`, `src/third_party/blink/renderer/modules/webgpu/*`, `src/gpu/command_buffer/service/*`, `src/components/viz/*` | This prepares the investigation checklist but does not close any optimization class until a built fork can be tested and benchmarked. |
| O-024 | Expand subsystem removal register | Kept for documentation, not performance evidence | `docs/removed_subsystems.md` now lists already-avoided Chrome product UI/services, draft viewer-entrypoint behavior, content-shell services still present, web-platform removal candidates, GPU/process experiments, and explicitly kept required subsystems | Low: documentation-only; risk is overclaiming avoided-by-target behavior as measured optimization | `docs/removed_subsystems.md`, `docs/source_investigation.md` | No subsystem is marked physically removed yet; every pending row still requires build, smoke, same-revision benchmark subset, and optimization-log update. |
| O-025 | Normalize raw Windows viewer paths in the draft Chromium patch | Draft patch kept; runtime pending | `git -C src apply --check ..\chromium_patches\0001-draft-minimal-three-viewer-entrypoint.patch` passes after changing `--viewer-app-url` handling to treat absolute `GetSwitchValuePath` values as file launches before generic `GURL` parsing | Low to medium: startup URL parsing affects package launch and navigation lock behavior | `chromium_patches/0001-draft-minimal-three-viewer-entrypoint.patch`, `chromium_patches/minimal_viewer_entrypoint.md`, `scripts/run_file_navigation_lock_tests.mjs`, `scripts/stage_viewer_package.ps1` | Prevents `C:\...` package paths from being misclassified as URL-like values. Fork runtime file-navigation smoke is still pending the binary. |
| O-026 | Emit a machine-readable prebuild environment manifest | Kept for workflow | `verify_prebuild.ps1 -AllowMissingAtl` writes `benchmarks/reports/prebuild-environment.json`, which records the pinned revision, `src` status, viewer patch state, GN arg hashes, expected stock/fork binary paths, non-admin shell state, non-loopback IPv4 state for external navigation smoke, missing ATL/MFC header check, `vswhere -requires Microsoft.VisualStudio.Component.VC.ATLMFC` component probe, and Siso build-failure freshness metadata | Low: evidence-capture only | `scripts/write_environment_manifest.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `benchmarks/reports/prebuild-environment.json`, `docs/build.md` | The manifest currently has `ok: false` because `visual_studio_atl` and the ATL/MFC component probe are missing; that is expected until the elevated ATL installer is run. |
| O-027 | Regression-test audit behavior for already-applied fork patch state | Kept for workflow | `scripts/test_patch_state_audit.ps1` passes: it uses temporary test hooks to make `write_environment_manifest.ps1` record `patch.already_applied=true`, then runs `audit_artifacts.ps1 -PatchStateOnly` to verify the audit accepts that state without applying the patch to `src` | Low: workflow-test only; test hooks are scoped to temporary manifest output and patch-state-only audit mode | `scripts/test_patch_state_audit.ps1`, `scripts/audit_artifacts.ps1`, `scripts/write_environment_manifest.ps1`, `benchmarks/tmp/patch-state-audit-test` | Prevents the post-ATL pipeline from failing the final audit simply because the fork patch has already been applied for the fork build, while avoiding stale report artifacts or source-tree mutation during verification. |
| O-028 | Stamp fork benchmark results with a patch-derived fork revision | Kept for workflow | `run_official_comparison.ps1`, `run_trusted_experiment_matrix.ps1`, and `run_post_atl_pipeline.ps1` now compute `<chromium_revision>+viewerpatch-<patch sha>` and pass it to fork benchmark JSON; official suite validation checks the exact expected value for fork results | Low: metadata-only; risk is only stale hashes if a runner bypasses these scripts | `scripts/run_official_comparison.ps1`, `scripts/run_trusted_experiment_matrix.ps1`, `scripts/run_post_atl_pipeline.ps1`, `scripts/validate_benchmark_suite.mjs`, `docs/benchmark_methodology.md` | Prevents official fork results from being indistinguishable from the unmodified upstream commit when `src` remains pinned to Chromium. |
| O-029 | Validate trusted experiment matrix suites before reporting | Kept for workflow | `run_trusted_experiment_matrix.ps1 -DryRun` prints per-experiment `validate_benchmark_suite.mjs` commands with `--expectedScenes`, checkout/build-args requirements, smoke rejection, and exact `--expectedForkRevision`; `-BuildArgs` is now required | Low: validation-only; risk is rejecting ad hoc local experiments that lack reproducible build metadata | `scripts/run_trusted_experiment_matrix.ps1`, `scripts/validate_benchmark_suite.mjs`, `docs/benchmark_methodology.md` | Prevents trusted flag summaries from being generated from partial scene sets, stale fork metadata, installed-browser smoke artifacts, or results without build-args hashes. |
| O-030 | Fail full-suite runs on individual scene errors and validate each result | Kept for workflow | `run_full_suite.ps1` now throws when a scene benchmark exits non-zero and runs `validate_metrics.mjs` on each emitted JSON before continuing | Low: validation-only; catches failures earlier than the later complete-suite gate | `scripts/run_full_suite.ps1`, `scripts/validate_metrics.mjs`, `docs/benchmark_methodology.md` | Prevents a failed middle scene from being masked by a later successful scene in stock/fork official suite runs. |
| O-031 | Use exact current-run benchmark files for official and trusted reports | Kept for workflow | `run_official_comparison.ps1 -DryRun` and `run_trusted_experiment_matrix.ps1 -DryRun` now pass explicit scene JSON paths to validation, summary, and comparison commands instead of broad result globs | Low: reporting-only; exact file paths make stale-output failures more obvious | `scripts/run_official_comparison.ps1`, `scripts/run_trusted_experiment_matrix.ps1`, `docs/benchmark_methodology.md` | Prevents older long-stability, partial experiment, or previous-label outputs from being included in a new official or trusted comparison report. |
| O-032 | Prevent stale generated GN args from masquerading as reproducible builds | Kept for workflow | `build_chromium.ps1` now fails when an existing output `args.gn` differs from the checked-in args template unless `-OverwriteArgs` is passed; `audit_artifacts.ps1` hashes generated baseline/fork args against their templates | Low: build-workflow guard; may require intentional `-OverwriteArgs` for local experiments | `scripts/build_chromium.ps1`, `scripts/audit_artifacts.ps1`, `docs/build.md` | Ensures build evidence is tied to the committed GN args rather than whatever happened to be left in `src/out`. |
| O-033 | Regression-test stale GN args rejection | Kept for workflow | `test_build_args_guard.ps1` creates a temporary stale `src\out\ArgsGuardTest-*\args.gn`, verifies `build_chromium.ps1` rejects it before build generation, and cleans up; `verify_prebuild.ps1 -AllowMissingAtl` runs this test | Low: test-only; touches only a guarded temporary output directory under `src\out` | `scripts/test_build_args_guard.ps1`, `scripts/verify_prebuild.ps1`, `scripts/build_chromium.ps1` | Keeps the reproducible-build guard from regressing while the main Chromium build remains blocked by ATL/MFC. |
| O-034 | Fail build entrypoints on host prerequisites before mutating fork state | Kept for workflow | `build_chromium.ps1` now runs `check_prereqs.ps1` before creating or refreshing output args, and `build_viewer_fork.ps1` runs it before applying the viewer patch; only the stale-args regression test uses `-SkipPrereqCheck` | Low: workflow guard; direct builds now stop earlier when ATL/MFC is missing | `scripts/build_chromium.ps1`, `scripts/build_viewer_fork.ps1`, `scripts/check_prereqs.ps1`, `docs/build.md` | Prevents direct fork-build attempts from patching `src` and then failing on a known host prerequisite. |
| O-035 | Emit an official comparison manifest | Kept for workflow | `run_official_comparison.ps1 -DryRun` writes `official-comparison-manifest.dry-run.json` with binaries, build args, package dirs, patch-derived fork revision, options, exact result files, report files, trace files, validation requirements, and artifact metadata; audit requires a completed non-dry manifest with browser/build-args hashes | Low: metadata-only | `scripts/run_official_comparison.ps1`, `scripts/audit_artifacts.ps1`, `docs/benchmark_methodology.md` | Makes the official stock/fork benchmark evidence reproducible and easier to audit after the Chromium binaries exist. |
| O-036 | Make ATL/MFC blocker remediation explicit | Kept for workflow | `check_prereqs.ps1` now reports the Visual Studio Installer path, the `Microsoft.VisualStudio.Component.VC.ATLMFC` install component, the missing `atldef.h` header, and a `vswhere -requires` component probe; `install_vs_atl.ps1` prints the post-install verification and pipeline commands | Low: documentation and diagnostics only | `scripts/check_prereqs.ps1`, `scripts/install_vs_atl.ps1`, `scripts/write_environment_manifest.ps1`, `docs/build.md`, `docs/known_limitations.md` | Reduces ambiguity in the current host blocker without changing Chromium source or build flags. |
| O-037 | Emit a trusted experiment matrix manifest with artifact hashes | Kept for workflow | `run_trusted_experiment_matrix.ps1 -DryRun` writes `trusted-experiment-matrix-manifest.dry-run.json` with planned phase, patch-derived fork revision, exact per-experiment result paths, report paths, suite-validation requirements, and artifact metadata; audit now requires a completed non-dry manifest with current fork revision, browser/build-args hashes, hashed result JSON, and summary/comparison reports | Low: metadata-only | `scripts/run_trusted_experiment_matrix.ps1`, `scripts/audit_artifacts.ps1`, `docs/benchmark_methodology.md`, `docs/build.md` | Makes risky trusted-content flag measurements auditable independently from the official stock/fork comparison. |
| O-038 | Regression-test the post-ATL dry-run handoff | Kept for workflow | `test_post_atl_pipeline_dry_run.ps1` runs `run_post_atl_pipeline.ps1 -DryRun` and asserts the output includes the official comparison command, trusted experiment matrix command, package directory, build args, patch-derived fork revision, WebGPU/aggressive/ANGLE options, trusted-content experiment flags, and the patched-source baseline guard; `verify_prebuild.ps1 -AllowMissingAtl` runs this check | Low: orchestration-test only; does not execute builds or benchmarks | `scripts/test_post_atl_pipeline_dry_run.ps1`, `scripts/run_post_atl_pipeline.ps1`, `scripts/verify_prebuild.ps1`, `docs/build.md` | Prevents the one-command post-ATL resume path from silently dropping benchmark, trusted-experiment, or clean-baseline evidence steps while the actual build remains blocked by ATL/MFC. |
| O-039 | Include post-ATL dry-run test evidence in the artifact audit | Kept for workflow | `audit_artifacts.ps1` now runs `test_post_atl_pipeline_dry_run.ps1` and adds an explicit checklist row showing whether the one-command post-ATL dry-run handoff passes, rather than only listing the test file as present | Low: audit-only; adds a cheap subprocess to audit generation | `scripts/audit_artifacts.ps1`, `scripts/test_post_atl_pipeline_dry_run.ps1`, `docs/prompt_to_artifact_checklist.md` | Makes the generated prompt-to-artifact checklist distinguish executable orchestration evidence from file-existence evidence. |
| O-040 | Regression-test trusted-content gating for unsafe viewer aliases | Kept for workflow | `test_viewer_patch_trusted_gates.ps1` verifies the draft patch defines the requested viewer gates, calls `ConfigureViewerTrustedContentSwitches`, early-returns unless both `--viewer-app-url` and `--viewer-trusted-content` are present, keeps unsafe native aliases such as `--in-process-gpu`, `--single-process`, WebGPU developer flags, high-performance GPU, ANGLE backend, pass-through command decoder, and file-access relaxation inside that guarded function, and leaves the remaining Blink/direct-presentation gates as no-ops | Low: static patch audit only; runtime behavior still requires the fork binary and smoke tests | `scripts/test_viewer_patch_trusted_gates.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `docs/trusted_content_flags.md` | Prevents accidental ungated unsafe optimizations outside the single-purpose viewer path while preserving the rule that measured runtime evidence is still required before keeping any aggressive mode. |
| O-041 | Regression-test planned dry-run benchmark manifests | Kept for workflow | `test_dry_run_manifests.ps1` runs `run_official_comparison.ps1 -DryRun` and `run_trusted_experiment_matrix.ps1 -DryRun`, then validates the generated planned manifests for current patch-derived fork revision, required scene coverage, exact result counts, report/trace paths, suite-validation settings, trusted experiment labels, and viewer patch metadata hashes; it also verifies dry-run report artifacts are restored/cleaned after the test; `verify_prebuild.ps1 -AllowMissingAtl` and `audit_artifacts.ps1` run the test | Low: manifest-structure test only; does not execute benchmarks or prove performance | `scripts/test_dry_run_manifests.ps1`, `scripts/run_official_comparison.ps1`, `scripts/run_trusted_experiment_matrix.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `docs/benchmark_methodology.md` | Prevents planned-run evidence from drifting away from the official stock/fork and trusted-experiment requirements before the ATL-blocked binaries exist. |
| O-042 | Regression-test draft navigation-lock patch structure | Kept for workflow | `test_viewer_patch_navigation_lock.ps1` statically verifies the draft patch defines `--viewer-app-url` and `--viewer-block-external-navigation`, hides the toolbar in viewer startup mode, handles URL and absolute local path startup values, blocks non-current-tab opens, registers `ViewerNavigationThrottle` only under the navigation-lock switch, allows about:blank, permits same-origin HTTP navigation, and confines file navigation to the viewer file or children of its directory | Low: static patch audit only; runtime behavior still requires fork HTTP/file navigation lock smoke tests | `scripts/test_viewer_patch_navigation_lock.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `chromium_patches/0001-draft-minimal-three-viewer-entrypoint.patch`, `docs/benchmark_methodology.md`, `docs/removed_subsystems.md` | Reduces the chance of losing navigation confinement while the fork runtime build is blocked, without treating static checks as runtime proof. |
| O-043 | Reject missing GPU metadata and known software-rendered GPU paths in strict official reports | Kept for workflow | `compare_results.mjs --strictOfficial` now rejects inputs with missing GPU/backend metadata or whose GPU, driver, or ANGLE metadata indicate SwiftShader, WARP, llvmpipe, softpipe, software renderer/rasterizer, or Microsoft Basic Render Driver; `test_strict_official_rejects_software_rendering.ps1` verifies missing metadata and SwiftShader inputs fail while hardware-like metadata passes | Low: validation-only; ambiguous hardware metadata still needs trace/system-info review in real runs | `scripts/compare_results.mjs`, `scripts/test_strict_official_rejects_software_rendering.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `docs/benchmark_methodology.md`, `docs/known_limitations.md` | Prevents official stock/fork performance reports from accidentally using unknown or software-rendered primary paths while preserving smoke artifacts for diagnostics. |
| O-044 | Reject missing GPU metadata and known software-rendered GPU paths during suite validation | Kept for workflow | `validate_benchmark_suite.mjs` now supports `--rejectSoftwareRendering` and `--requireGpuMetadata`; `run_official_comparison.ps1` and `run_trusted_experiment_matrix.ps1` pass both flags for every official/trusted suite validation; `test_benchmark_suite_rejects_software_rendering.ps1` verifies missing metadata and SwiftShader inputs fail while hardware-like metadata passes | Low: validation-only; ambiguous hardware metadata still needs trace/system-info review in real runs | `scripts/validate_benchmark_suite.mjs`, `scripts/run_official_comparison.ps1`, `scripts/run_trusted_experiment_matrix.ps1`, `scripts/test_benchmark_suite_rejects_software_rendering.ps1`, `scripts/audit_artifacts.ps1`, `docs/benchmark_methodology.md` | Catches unknown or software-rendered inputs before summaries/comparisons are generated, including trusted experiment matrix reports that do not use `--strictOfficial`. |
| O-045 | Enforce stability resource-growth thresholds | Kept for workflow | `validate_stability_result.mjs` checks positive FPS, measured frame samples, RSS delta thresholds, renderer geometry/texture/program delta thresholds, and expected viewer flag metadata; `run_long_stability.ps1` calls it after metric validation; `test_stability_result_thresholds.ps1` verifies stable v18 counters pass while RSS growth, renderer program growth, missing renderer deltas, and mismatched viewer metadata fail | Low: validation-only; final evidence still requires one-hour stock/fork runs after binaries exist | `scripts/validate_stability_result.mjs`, `scripts/run_long_stability.ps1`, `scripts/test_stability_result_thresholds.ps1`, `scripts/run_post_atl_pipeline.ps1`, `docs/stability_behavior.md`, `docs/benchmark_methodology.md` | Converts RSS and renderer resource counters plus viewer launch metadata from passive diagnostics into enforceable long-run stability gates for stable scenes such as `instancing`. |
| O-046 | Enforce expected benchmark duration and warmup in suite validation | Kept for workflow | `validate_benchmark_suite.mjs` now supports `--expectedMeasuredSeconds` and `--expectedWarmupSeconds`; official and trusted suite validations pass the workflow duration and warmup values; `test_benchmark_suite_duration_validation.ps1` verifies matching values pass while mismatched measured or warmup seconds fail | Low: validation-only; final evidence still requires real stock/fork runs | `scripts/validate_benchmark_suite.mjs`, `scripts/run_official_comparison.ps1`, `scripts/run_trusted_experiment_matrix.ps1`, `scripts/test_benchmark_suite_duration_validation.ps1`, `scripts/test_dry_run_manifests.ps1`, `docs/benchmark_methodology.md` | Prevents short smoke-style JSON with official-looking labels from being accepted as full official or trusted benchmark evidence. |
| O-047 | Reject duration and warmup mismatches in strict official comparison | Kept for workflow | `compare_results.mjs --strictOfficial` now rejects baseline/fork inputs for the same scene/renderer when `measured_seconds` or `warmup_seconds` differ; `test_strict_official_rejects_software_rendering.ps1` verifies both mismatch cases fail | Low: validation-only; suite validation still provides the exact expected duration/warmup gate before report generation | `scripts/compare_results.mjs`, `scripts/test_strict_official_rejects_software_rendering.ps1`, `docs/benchmark_methodology.md` | Adds a direct-report safeguard for accidental comparisons across different measurement windows. |
| O-048 | Require fork revision identity in strict official comparison | Kept for workflow | `compare_results.mjs --strictOfficial` now rejects fork-labeled result JSON without `fork_revision`, more than one fork revision across fork inputs, or a fork revision not derived from the shared `chromium_revision`; `test_strict_official_rejects_software_rendering.ps1` covers missing and wrong-base fork revisions | Low: validation-only; suite validation and manifests still carry the exact expected patch-derived fork revision | `scripts/compare_results.mjs`, `scripts/test_strict_official_rejects_software_rendering.ps1`, `docs/benchmark_methodology.md` | Prevents a direct official report from treating an unidentified or stale fork binary as valid comparison evidence. |
| O-049 | Regression-test the final artifact audit gate | Kept for workflow | `test_post_atl_pipeline_dry_run.ps1` now asserts `-FinalGate` reaches `audit_artifacts.ps1 -FailOnIncomplete`; `test_audit_fail_on_incomplete.ps1` verifies the current incomplete artifact set fails that gate after writing a checklist; `verify_prebuild.ps1 -AllowMissingAtl` runs both checks | Low: workflow-test only; intentionally expects failure from the final gate until stock/fork binaries and official evidence exist | `scripts/test_post_atl_pipeline_dry_run.ps1`, `scripts/test_audit_fail_on_incomplete.ps1`, `scripts/audit_artifacts.ps1`, `scripts/verify_prebuild.ps1`, `docs/build.md` | Prevents the post-ATL resume path from silently finishing with missing binaries, official benchmark reports, trusted matrix evidence, or one-hour stability artifacts. |
| O-050 | Require WebGPU result and report hashes in completed official manifests | Kept for workflow | `audit_artifacts.ps1` now rejects completed official manifests that include WebGPU but lack complete hashed WebGPU stock/fork result metadata or the WebGPU comparison report hash; `test_official_manifest_webgpu_audit.ps1` synthesizes that stale-manifest case and verifies the audit marks it pending | Low: audit-only; no runtime effect | `scripts/audit_artifacts.ps1`, `scripts/test_official_manifest_webgpu_audit.ps1`, `scripts/verify_prebuild.ps1`, `docs/benchmark_methodology.md` | Prevents future final audits from accepting older real manifests that predate the WebGPU evidence hash requirements. |
| O-051 | Align artifact-audit suite rows with official suite validation gates | Kept for workflow | `audit_artifacts.ps1` now passes software-renderer rejection and GPU metadata requirements into all official stock/fork WebGL2/WebGPU suite rows, and passes expected duration/warmup from a completed official manifest when available; `test_audit_official_suite_validation_gates.ps1` statically verifies that wiring | Low: audit-only; final evidence still depends on real stock/fork result files | `scripts/audit_artifacts.ps1`, `scripts/test_audit_official_suite_validation_gates.ps1`, `scripts/verify_prebuild.ps1`, `docs/benchmark_methodology.md` | Prevents manual or stale raw JSON files from satisfying final checklist suite rows without the same strict evidence policy used by `run_official_comparison.ps1`. |
| O-052 | Regression-test viewer runtime API surface and primary-path readback avoidance | Kept for workflow | `test_viewer_runtime_surface.ps1` verifies WebGL2/WebGPU renderer creation, `requestAnimationFrame`, `performance.now`, resize/pointer input, fetch/createImageBitmap texture loading, TextureLoader fallback, GLTFLoader, benchmark result export, scene/renderer/duration/warmup/output CLI/query handoff, viewer URL/navigation-lock handoff, and rejects `readPixels`/`getImageData`/`toDataURL` in `viewer/src`; `verify_prebuild.ps1 -AllowMissingAtl` runs it | Low: static source audit only; runtime smoke and official same-revision measurements remain required | `scripts/test_viewer_runtime_surface.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `viewer/src/main.js`, `viewer/src/renderers.js`, `viewer/src/scenes.js`, `scripts/run_benchmark.mjs`, `docs/benchmark_methodology.md` | Prevents accidental loss of the prompt-required local viewer API surface while the Chromium build remains blocked by ATL/MFC. |
| O-053 | Add explicit nullable compositor and presentation latency metrics | Kept for workflow | `run_benchmark.mjs`, `validate_metrics.mjs`, `BenchmarkRecorder`, summaries, comparisons, and artifact audit now require `avg_compositor_latency_ms` and `avg_presentation_latency_ms`; current smoke JSON artifacts are backfilled with `null` until trace-derived or browser-exposed values are available | Low: schema/reporting change only; null fields are not performance evidence | `viewer/src/metrics.js`, `scripts/run_benchmark.mjs`, `scripts/validate_metrics.mjs`, `scripts/summarize_results.mjs`, `scripts/compare_results.mjs`, `scripts/audit_artifacts.ps1`, `docs/benchmark_methodology.md` | Prevents the required compositor/presentation latency surface from being omitted from future official benchmark JSON, while keeping trace files as the source of truth when the fields are unavailable. |
| O-054 | Regression-test runtime smoke harness coverage | Kept for workflow | `test_smoke_coverage.ps1` verifies `run_smoke_tests.mjs` and `validate_smoke_result.mjs` still cover viewer launch, WebGL2 context creation, `requestAnimationFrame`, Canvas, local `fetch`, `performance.now`, basic pointer/wheel/keyboard input events, WebGPU adapter/device creation, context/device loss signals, Three.js cube render, texture/createImageBitmap, shader material, benchmark result capture, browser-exit crash detection, required-pass checks, allowed-skip checks, and missing-test rejection; `verify_prebuild.ps1 -AllowMissingAtl` runs it | Low: static harness audit only; stock/fork smoke JSON evidence remains required after binaries exist | `scripts/test_smoke_coverage.ps1`, `scripts/run_smoke_tests.mjs`, `scripts/validate_smoke_result.mjs`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `docs/benchmark_methodology.md` | Prevents a future harness edit from silently dropping one of the prompt-required automated runtime checks before the post-ATL official comparison is run. |
| O-055 | Regression-test coverage of every requested optimization class | Kept for workflow | `removed_subsystems.md` now contains a prompt optimization class tracking table, and `test_optimization_tracking.ps1` verifies the documentation corpus mentions every requested class, including Safe Browsing, background networking, ANGLE/platform backends, WebGL validation, WebGPU pipeline caching, compositor/direct presentation, Blink/DOM removal, media/printing/PDF/WebRTC/accessibility/password/payments, V8 flags, memory allocator, and process model choices | Low: documentation/audit-only; does not mark any optimization class as measured | `docs/removed_subsystems.md`, `scripts/test_optimization_tracking.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `docs/benchmark_methodology.md` | Prevents the final audit from claiming an optimization class is tracked when it has silently fallen out of the docs before measured post-build experiments. |
| O-056 | Validate benchmark details inside runtime smoke results | Kept for workflow | `validate_smoke_result.mjs --type runtime` now requires `benchmark_run.details` to identify the deterministic `many-draw-calls`/`webgl2` smoke run and report positive FPS, positive frame count, and non-negative startup time; `test_smoke_coverage.ps1` verifies those checks remain present | Low: validation-only; still does not replace same-revision stock/fork benchmark evidence | `scripts/validate_smoke_result.mjs`, `scripts/test_smoke_coverage.ps1`, `docs/benchmark_methodology.md` | Prevents a smoke artifact from satisfying runtime coverage with only a passed benchmark label and no usable performance sanity data. |
| O-057 | Validate exact trusted experiment flags in dry-run manifests and commands | Kept for workflow | `test_dry_run_manifests.ps1` now verifies every planned trusted experiment label maps to its expected runtime flags and that official/trusted dry-run commands include the viewer trusted-content, aggressive GPU, in-process GPU, single-process, ANGLE backend, relaxed WebGL pass-through decoder, and remaining reserved Blink/direct-presentation no-op gate switches where expected | Low: manifest and command-shape validation only; measured impact still requires the post-ATL fork binary | `scripts/test_dry_run_manifests.ps1`, `scripts/run_official_comparison.ps1`, `scripts/run_trusted_experiment_matrix.ps1`, `docs/benchmark_methodology.md` | Prevents planned evidence from preserving labels while silently dropping or changing the actual flags that define risky optimization gates. |
| O-058 | Require trusted flag mappings in completed trusted matrix audit | Kept for workflow | `audit_artifacts.ps1` now rejects completed trusted experiment matrix manifests unless they include the default, aggressive GPU, in-process GPU, single-process, ANGLE backend, relaxed WebGL validation, and reserved no-op gate experiments with the expected flag mappings; `test_trusted_manifest_flag_audit.ps1` verifies the audit rejects a synthetic completed manifest with the aggressive GPU flag omitted | Low: audit-only; final measured evidence still depends on the real fork binary and trusted matrix outputs | `scripts/audit_artifacts.ps1`, `scripts/test_trusted_manifest_flag_audit.ps1`, `scripts/verify_prebuild.ps1`, `docs/build.md` | Prevents the final artifact checklist from accepting a trusted experiment matrix that has complete-looking files and hashes but lacks evidence for the risky optimization gates requested in the prompt. |
| O-059 | Validate navigation smoke detail semantics | Kept for workflow | `validate_smoke_result.mjs` now checks navigation and file-navigation smoke details for viewer URL launch, allowed same-origin/file navigation, blocked HTTP/file navigation non-landing, blocked-origin hit count, external non-loopback HTTP blocking with zero request hits, and `window.open` page-count confinement; `test_smoke_detail_validation.ps1` proves valid synthetic artifacts pass while blocked-origin, external-HTTP landing, external-request-hit, loopback-external, and blocked-file false positives fail | Low: validation-only; fork runtime navigation smoke still requires the patched binary | `scripts/validate_smoke_result.mjs`, `scripts/test_smoke_detail_validation.ps1`, `scripts/test_smoke_coverage.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `docs/benchmark_methodology.md` | Prevents final runtime evidence from treating a pass-labelled navigation smoke result as proof unless its recorded details show the expected allowed and blocked URL outcomes. |
| O-060 | Require runtime smoke and navigation hashes in completed official manifests | Kept for workflow | `audit_artifacts.ps1` now rejects completed official comparison manifests generated with `skip_smoke` or `skip_navigation_lock`, or without two hashed runtime-smoke artifacts and two hashed navigation-lock artifacts; `test_official_manifest_runtime_audit.ps1` verifies missing runtime hashes, `skip_smoke=true`, `skip_navigation_lock=true`, and missing navigation-lock hashes are rejected | Low: audit-only; actual runtime evidence still requires stock/fork binaries | `scripts/audit_artifacts.ps1`, `scripts/test_official_manifest_runtime_audit.ps1`, `scripts/verify_prebuild.ps1`, `scripts/run_official_comparison.ps1`, `docs/benchmark_methodology.md` | Prevents the final official manifest from passing with complete benchmark JSON and reports but missing the prompt-required stock/fork runtime smoke and fork navigation-lock evidence. |
| O-061 | Require trace summary hashes in completed official manifests | Kept for workflow | `audit_artifacts.ps1` now rejects completed official comparison manifests with `capture_trace=true` unless both raw stock/fork traces and their trace summary reports are hashed; `test_official_manifest_trace_audit.ps1` verifies the audit rejects a synthetic trace manifest without trace summary hashes | Low: audit-only; actual trace evidence still requires stock/fork binaries | `scripts/audit_artifacts.ps1`, `scripts/test_official_manifest_trace_audit.ps1`, `scripts/verify_prebuild.ps1`, `scripts/run_official_comparison.ps1`, `docs/benchmark_methodology.md` | Prevents final trace evidence from containing only raw trace files without the generated human-readable summaries used to inspect shader, upload, GPU command, presentation, and JavaScript categories. |
| O-062 | Include minimal-target inspection helper in verification coverage | Kept for workflow | `inspect_chromium_target.ps1` is now included in prebuild PowerShell syntax checks and the artifact checklist's build/fork script row; build and source-investigation docs list it as the reproducible command for inspecting `content_shell` target declarations and candidate content-shell app/browser/common files | Low: documentation and syntax coverage only | `scripts/inspect_chromium_target.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `docs/build.md`, `docs/source_investigation.md` | Keeps the initial "locate the minimal Chromium target" step from becoming an undocumented one-off while the actual content-shell build remains blocked by ATL/MFC. |
| O-063 | Validate official smoke artifacts immediately after generation | Kept for workflow | `run_official_comparison.ps1` now runs `validate_smoke_result.mjs --type runtime` after stock/fork runtime smoke generation, `--type navigation` after fork HTTP navigation-lock smoke generation, and `--type file-navigation` after fork file-navigation smoke generation; `test_dry_run_manifests.ps1` asserts these commands appear in dry-run output | Low: orchestration-only; runtime artifacts still require stock/fork binaries | `scripts/run_official_comparison.ps1`, `scripts/validate_smoke_result.mjs`, `scripts/test_dry_run_manifests.ps1`, `docs/benchmark_methodology.md` | Fails the official comparison workflow before benchmark summaries if required smoke artifacts are malformed or lack the stricter runtime/navigation detail semantics. |
| O-064 | Pass explicit scene list to official suite validation | Kept for workflow | `run_official_comparison.ps1` now passes `--expectedScenes many-draw-calls,instancing,shader-heavy,texture-streaming,postprocessing,large-static,gltf-loader-stress` to every official stock/fork/aggressive/WebGPU suite validation; `test_dry_run_manifests.ps1` asserts the expected scene list appears in official validation commands, and `audit_artifacts.ps1` checks the wiring | Low: validation-only; official suite artifacts still require stock/fork binaries | `scripts/run_official_comparison.ps1`, `scripts/validate_benchmark_suite.mjs`, `scripts/test_dry_run_manifests.ps1`, `scripts/audit_artifacts.ps1`, `docs/benchmark_methodology.md` | Makes the official suite contract explicit in command output rather than relying on validator defaults or filename conventions. |
| O-065 | Regression-test viewer package staging before fork binary exists | Kept for workflow | `test_stage_viewer_package.ps1` creates a disposable synthetic Chromium output under `benchmarks/packages`, stages it through `stage_viewer_package.ps1 -Clean`, checks required files and launcher flags, and is now run by `verify_prebuild.ps1` and `audit_artifacts.ps1` | Low: test-only; touches only guarded temporary directories under `benchmarks/packages` | `scripts/test_stage_viewer_package.ps1`, `scripts/stage_viewer_package.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `docs/benchmark_methodology.md` | Catches packaging regressions before the ATL-blocked fork binary exists; final package evidence still requires a real patched `content_shell.exe`. |
| O-066 | Require package metadata in completed manifests when package dirs are supplied | Kept for workflow | `audit_artifacts.ps1` now rejects completed official and trusted manifests when `package_dir`/`package_dirs` is set but the corresponding package metadata is missing or empty; `test_manifest_package_audit.ps1` verifies both rejection paths with synthetic completed manifests | Low: audit-only; no runtime effect | `scripts/audit_artifacts.ps1`, `scripts/test_manifest_package_audit.ps1`, `scripts/verify_prebuild.ps1`, `docs/benchmark_methodology.md` | Prevents a final report from claiming package-size evidence when the package staging path was requested but did not actually produce a non-empty package. |
| O-067 | Require package size values in packaged benchmark suites | Kept for workflow | `validate_benchmark_suite.mjs --requirePackageSize` rejects missing, null, zero, or negative `package_size_mb`; official and trusted runners pass the flag when a package directory is supplied; `test_benchmark_suite_package_validation.ps1` and `test_dry_run_manifests.ps1` verify the behavior | Low: validation-only; packaged evidence still requires real fork package staging after build | `scripts/validate_benchmark_suite.mjs`, `scripts/run_official_comparison.ps1`, `scripts/run_trusted_experiment_matrix.ps1`, `scripts/test_benchmark_suite_package_validation.ps1`, `scripts/test_dry_run_manifests.ps1`, `docs/benchmark_methodology.md` | Prevents packaged official/trusted results from passing suite validation with a nullable package-size field after package staging was requested. |
| O-068 | Validate one-hour stability evidence rather than counting filenames | Kept for workflow | `validate_stability_result.mjs` now supports duration, warmup, renderer/scene/variant, checkout/build metadata, fork revision, GPU metadata, and software-renderer gates; `audit_artifacts.ps1` uses those gates for stock/fork one-hour stability rows; `test_stability_result_thresholds.ps1` verifies short-run and missing-fork failures | Low: validation-only; final evidence still requires real one-hour stock/fork runs | `scripts/validate_stability_result.mjs`, `scripts/audit_artifacts.ps1`, `scripts/test_stability_result_thresholds.ps1`, `docs/benchmark_methodology.md`, `docs/stability_behavior.md` | Prevents a short or stale `*long-stability*.json` file from satisfying the stability completion rows by filename alone. |
| O-069 | Add GN generation-only mode for reproducible args evidence | Kept for workflow | `build_chromium.ps1 -GenOnly` runs `gn gen` and stops before `autoninja`; `test_gn_gen_only.ps1` verifies generated args match the checked-in template, `build.ninja` exists, and no `content_shell.exe` binary is produced | Low: build metadata only; `-GenOnly` is not build evidence and final binaries still require ATL/MFC | `scripts/build_chromium.ps1`, `scripts/test_gn_gen_only.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `docs/build.md` | Allows fork GN args to be generated and audited while the compile step remains blocked by missing ATL headers, without applying the viewer patch or claiming runtime completion. |
| O-070 | Regression-test ATL remediation and post-install handoff | Kept for workflow | `install_vs_atl.ps1` and `check_prereqs.ps1` now print the completion-oriented post-ATL command with WebGPU, aggressive GPU, trace capture, trusted experiment matrix including D3D11 ANGLE coverage, one-hour stability, and final audit switches; `test_atl_remediation_handoff.ps1` statically verifies the elevation guard, ATL/MFC component id, installer switches, verifier command, and full post-ATL handoff in the scripts plus `README.md` and `docs/build.md` | Low: remediation and workflow text only; installing ATL still requires an elevated shell | `scripts/install_vs_atl.ps1`, `scripts/check_prereqs.ps1`, `scripts/test_atl_remediation_handoff.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `README.md`, `docs/build.md` | Prevents the current hard-blocker remediation path from guiding the user into a shortened post-install run that would omit required trusted experiments, ANGLE backend coverage, long stability, or final audit evidence. |
| O-071 | Audit generated trusted/aggressive GN args separately | Kept for workflow | `audit_artifacts.ps1` now checks `src/out/ReleaseViewerTrustedAggressive/args.gn` against `build/gn_args/fork_trusted_aggressive.gn`, and `write_environment_manifest.ps1` records the generated file hash | Low: build metadata only; runtime trusted/aggressive evidence still requires the patched fork binary and explicit trusted-content flags | `scripts/audit_artifacts.ps1`, `scripts/write_environment_manifest.ps1`, `docs/build.md` | Keeps the experimental GN profile from being merely a checked-in template with no generated output evidence. |
| O-072 | Regression-test prebuild environment manifest coverage | Kept for workflow | `test_environment_manifest_coverage.ps1` writes a disposable environment manifest and verifies revision fields, patch state, Visual Studio instance metadata, ATL header/component metadata, non-loopback IPv4 state for external navigation smoke, all checked-in/generated GN arg entries, expected binary path entries, prerequisite checks, build-failure slots, and synthetic stale-vs-fresh Siso failure freshness detection | Low: manifest-schema validation only; does not claim build success while ATL is missing | `scripts/write_environment_manifest.ps1`, `scripts/test_environment_manifest_coverage.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `docs/build.md` | Prevents `prebuild-environment.json` from silently dropping evidence needed to explain the current host blocker, runtime navigation-smoke prerequisites, and reproduce the post-ATL build state. |
| O-073 | Regression-test required metric schema consistency | Kept for workflow | `test_metric_schema_consistency.ps1` verifies the required metric field sequence is identical in `run_benchmark.mjs`, `validate_metrics.mjs`, `audit_artifacts.ps1`, and `docs/benchmark_methodology.md`; `verify_prebuild.ps1` and the artifact audit run it | Low: schema validation only; official measured values still require stock/fork Chromium binaries | `scripts/test_metric_schema_consistency.ps1`, `scripts/run_benchmark.mjs`, `scripts/validate_metrics.mjs`, `scripts/audit_artifacts.ps1`, `docs/benchmark_methodology.md`, `docs/build.md` | Prevents a future runner, validator, audit, or documentation edit from silently dropping metrics required for same-revision performance claims. |
| O-074 | Guard stock baseline build against patched source state | Kept for workflow | `run_post_atl_pipeline.ps1` refuses to build the stock `ReleaseBaseline` output when the viewer patch is already applied, unless `-SkipBaselineBuild` is explicit; `test_post_atl_pipeline_dry_run.ps1` verifies the rejection and `-SkipBaselineBuild` acknowledgement through a dry-run-only patch-state override instead of mutating `src` | Low: workflow guard only; baseline binary still must be built and benchmarked after ATL/MFC is installed | `scripts/run_post_atl_pipeline.ps1`, `scripts/test_post_atl_pipeline_dry_run.ps1`, `docs/build.md`, `docs/benchmark_methodology.md` | Prevents a rerun after fork patch application from accidentally refreshing the stock baseline binary from patched source and invalidating same-revision stock/fork comparisons. |
| O-075 | Dispatch nested PowerShell scripts through `powershell.exe -File` | Kept for workflow | `run_post_atl_pipeline.ps1`, `run_official_comparison.ps1`, and `run_trusted_experiment_matrix.ps1` now dispatch child `.ps1` commands through `powershell.exe -NoProfile -ExecutionPolicy Bypass -File` so dynamically assembled named switches are parsed as named parameters instead of positional strings; `test_post_atl_pipeline_dry_run.ps1` statically asserts that dispatch path | Low: workflow invocation fix only; it changes orchestration mechanics, not Chromium runtime behavior | `scripts/run_post_atl_pipeline.ps1`, `scripts/run_official_comparison.ps1`, `scripts/run_trusted_experiment_matrix.ps1`, `scripts/test_post_atl_pipeline_dry_run.ps1` | Prevents post-ATL nested script calls such as `run_full_suite.ps1 -Duration ... -Warmup ...` from being rebound positionally and failing or running with incorrect parameters. |
| O-076 | Stamp trusted viewer flag metadata into raw benchmark JSON | Kept for workflow | `run_benchmark.mjs` now records every trusted viewer experiment flag, requested ANGLE backend, and pass-through browser flags in each result JSON; `validate_metrics.mjs` validates those optional fields and `test_benchmark_flag_metadata.ps1` verifies runner/validator coverage | Low: metadata-only; it does not change browser launch behavior or benchmark scoring | `scripts/run_benchmark.mjs`, `scripts/validate_metrics.mjs`, `scripts/test_benchmark_flag_metadata.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `docs/trusted_content_flags.md`, `docs/benchmark_methodology.md` | Keeps per-scene trusted experiment JSON self-describing even if copied away from `trusted-experiment-matrix-manifest.json`. |
| O-077 | Enforce expected trusted viewer flag metadata at suite-validation time | Kept for workflow | `validate_benchmark_suite.mjs` supports repeated `--expectedFlagMetadata key=value`; official and trusted workflows pass the expected stock/fork/trusted metadata for each suite, dry-run manifests record the expectations, and `test_benchmark_suite_flag_metadata.ps1` verifies matching, mismatched, and missing metadata cases | Low: validation-only; it rejects bad evidence but does not change runtime launch behavior | `scripts/validate_benchmark_suite.mjs`, `scripts/run_official_comparison.ps1`, `scripts/run_trusted_experiment_matrix.ps1`, `scripts/test_benchmark_suite_flag_metadata.ps1`, `scripts/test_dry_run_manifests.ps1`, `docs/benchmark_methodology.md`, `docs/trusted_content_flags.md` | Prevents a trusted experiment label or manifest entry from passing with raw result JSON that was launched with different viewer flags. Direct final-audit suite rows also repeat those metadata gates. |
| O-078 | Delay viewer benchmark startup for trace capture | Kept for workflow | `run_trace_capture.mjs` now passes `startDelayMs` to the viewer and records `start_delay_ms` in the trace sidecar; `viewer/src/main.js` waits before renderer/context creation; `test_trace_capture_start_delay.ps1` verifies the wiring | Low: trace-only workflow change; benchmark suite runs through `run_benchmark.mjs` keep zero delay by default | `viewer/src/main.js`, `scripts/run_trace_capture.mjs`, `scripts/test_trace_capture_start_delay.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `docs/benchmark_methodology.md`, `docs/build.md` | Prevents fork viewer-mode traces from missing early startup GPU work because `--viewer-app-url` began loading before CDP tracing attached. |
| O-079 | Require trace benchmark sidecar hashes in official manifests | Kept for workflow | `run_official_comparison.ps1` records `.result.json` trace sidecar paths and metadata; `audit_artifacts.ps1` rejects completed trace manifests without stock/fork sidecar hashes; `test_official_manifest_trace_audit.ps1` verifies missing sidecars and missing summaries are both rejected | Low: audit/manifest-only; runtime trace capture behavior is unchanged | `scripts/run_official_comparison.ps1`, `scripts/audit_artifacts.ps1`, `scripts/test_official_manifest_trace_audit.ps1`, `scripts/test_dry_run_manifests.ps1`, `docs/benchmark_methodology.md` | Keeps raw trace evidence tied to the exact benchmark scene, renderer, duration, warmup, and trace startup delay recorded by `run_trace_capture.mjs`. |
| O-080 | Validate trace benchmark sidecar contents | Kept for workflow | `validate_trace_result.mjs` checks trace `.result.json` sidecars against expected browser executable, scene, renderer, duration, warmup, start delay, launch flag metadata, and embedded benchmark metadata; `audit_artifacts.ps1` invokes it for completed official trace manifests; `test_trace_result_validation.ps1` and `test_official_manifest_trace_audit.ps1` verify mismatched metadata is rejected | Low: validation-only; it does not change trace capture or browser runtime behavior | `scripts/validate_trace_result.mjs`, `scripts/test_trace_result_validation.ps1`, `scripts/audit_artifacts.ps1`, `scripts/test_official_manifest_trace_audit.ps1`, `scripts/run_official_comparison.ps1`, `scripts/run_post_atl_pipeline.ps1` | Prevents a hashed sidecar from passing the final audit if it belongs to a different browser, trace scene, renderer, run duration, warmup, trace startup-delay, or stock/fork launch-mode configuration. |
| O-081 | Validate raw trace file structure before accepting official trace evidence | Kept for workflow | `validate_trace_file.mjs` checks Chrome trace JSON has a non-empty `traceEvents` array with named, phased, timestamped events; `audit_artifacts.ps1` invokes it for completed official trace manifests; `test_trace_file_validation.ps1` and `test_official_manifest_trace_audit.ps1` verify empty, malformed, unnamed, and invalid official raw traces are rejected | Low: validation-only; it does not change trace capture or browser runtime behavior | `scripts/validate_trace_file.mjs`, `scripts/test_trace_file_validation.ps1`, `scripts/audit_artifacts.ps1`, `scripts/test_official_manifest_trace_audit.ps1`, `docs/benchmark_methodology.md` | Prevents a hashed placeholder or corrupt trace JSON file from satisfying trace evidence requirements. |
| O-082 | Require official comparison report hashes in completed manifests | Kept for workflow | `audit_artifacts.ps1` rejects completed official manifests without hashed WebGL2 comparison report metadata, and also requires the WebGPU comparison report hash when `include_webgpu=true`; `test_official_manifest_report_audit.ps1` verifies both rejection paths | Low: audit-only; final measured evidence still requires stock/fork binaries and report generation | `scripts/audit_artifacts.ps1`, `scripts/test_official_manifest_report_audit.ps1`, `scripts/verify_prebuild.ps1`, `docs/benchmark_methodology.md` | Prevents a completed manifest with raw result hashes from satisfying final evidence if the human-readable stock/fork comparison reports were not generated and hashed. |
| O-083 | Require backend-consistent aggressive WebGL2 evidence in completed official manifests | Kept for workflow | `audit_artifacts.ps1` rejects completed official manifests with `include_aggressive_gpu=true` unless the aggressive/trusted WebGL2 suite has all seven result files and hashes, and the aggressive label, expected flag metadata, and result filenames match the requested aggressive ANGLE backend; `test_official_manifest_aggressive_audit.ps1` verifies mismatched-backend and missing-hash rejection paths | Low: audit-only; final measured evidence still requires the fork binary and trusted/aggressive run | `scripts/audit_artifacts.ps1`, `scripts/test_official_manifest_aggressive_audit.ps1`, `scripts/verify_prebuild.ps1`, `docs/benchmark_methodology.md` | Prevents the final manifest from implying the prompt-required aggressive trusted-content profile was measured when only stock/default-fork WebGL2 evidence exists or when default-backend aggressive files are mislabeled as an ANGLE backend experiment. |
| O-084 | Require completed official manifest provenance metadata | Kept for workflow | `audit_artifacts.ps1` rejects dry-run or incomplete-phase official manifests, stale Chromium revisions, stale patch-derived fork revisions, mismatched duration/warmup validation settings, missing browser metadata, and missing build-args hashes; `test_official_manifest_provenance_audit.ps1` verifies those rejection paths | Low: audit-only; final measured evidence still requires stock/fork binaries | `scripts/audit_artifacts.ps1`, `scripts/test_official_manifest_provenance_audit.ps1`, `scripts/verify_prebuild.ps1`, `docs/benchmark_methodology.md` | Prevents a stale or planned manifest from satisfying final evidence just because its result/report hash sections look complete. |
| O-085 | Require completed trusted matrix provenance and artifact metadata | Kept for workflow | `audit_artifacts.ps1` rejects trusted experiment matrix manifests that are dry-run, incomplete-phase, stale-Chromium, stale-fork, duration/warmup mismatched, missing experiments, missing result files, missing browser/build-args metadata, missing result hashes, missing report files, or missing report hashes; `test_trusted_manifest_provenance_audit.ps1` verifies those rejection paths | Low: audit-only; final measured evidence still requires the fork binary and trusted matrix run | `scripts/audit_artifacts.ps1`, `scripts/test_trusted_manifest_provenance_audit.ps1`, `scripts/verify_prebuild.ps1`, `docs/benchmark_methodology.md` | Prevents the trusted experiment matrix from being accepted with planned, stale, partial, or unhashed experiment evidence. |
| O-086 | Verify completed manifest artifact paths and hashes against disk | Kept for workflow | `audit_artifacts.ps1` rejects completed official and trusted manifests whose browser, build-args, result, runtime-smoke, navigation-lock, trace, report, or package metadata does not match files/directories on disk; `test_manifest_artifact_path_audit.ps1` verifies fake completed manifests are rejected | Low: audit-only; hashes add small verification cost | `scripts/audit_artifacts.ps1`, `scripts/test_manifest_artifact_path_audit.ps1`, `scripts/verify_prebuild.ps1`, `docs/benchmark_methodology.md` | Prevents a manifest with synthetic `exists=true` and fake SHA values from satisfying final evidence. |
| O-087 | Keep dry-run manifest regression tests from polluting report artifacts | Kept for workflow | `test_dry_run_manifests.ps1` backs up/restores `official-comparison-manifest.dry-run.json` and `trusted-experiment-matrix-manifest.dry-run.json`, verifies restored existence and contents, then confirms its temporary backup directory is removed; stale 1-second dry-run manifests from earlier regression runs were removed | Low: test-hygiene only | `scripts/test_dry_run_manifests.ps1`, `benchmarks/reports`, `docs/benchmark_methodology.md` | Keeps `benchmarks/reports` reserved for intentional dry-runs and real post-ATL manifests instead of regression-test leftovers. |
| O-088 | Keep post-ATL patch-state regression testing non-mutating | Kept for workflow | `run_post_atl_pipeline.ps1` now has a dry-run-only `-DryRunAssumeViewerPatchAlreadyApplied` test hook, and `test_post_atl_pipeline_dry_run.ps1` uses it to exercise the patched-source baseline-build guard without applying the viewer patch to the real Chromium checkout | Low: test hook is rejected outside `-DryRun`; no Chromium runtime behavior changes | `scripts/run_post_atl_pipeline.ps1`, `scripts/test_post_atl_pipeline_dry_run.ps1`, `docs/benchmark_methodology.md` | Avoids leaving `src` patched if a long verifier or artifact audit is interrupted while preserving coverage of the clean-baseline guard. |
| O-089 | Replace stale patch-state checklist evidence with executable regression | Kept for workflow | `audit_artifacts.ps1` now runs `test_patch_state_audit.ps1` and reports its output instead of reading `benchmarks/reports/patch-state-test-checklist.md`; `verify_prebuild.ps1` also runs the focused test directly | Low: audit/test workflow only; no runtime effect | `scripts/audit_artifacts.ps1`, `scripts/test_patch_state_audit.ps1`, `scripts/verify_prebuild.ps1` | Prevents a stale generated checklist from being accepted as proof that already-applied patch states still work. |
| O-090 | Enforce pinned Chromium revision in suite and stability validation | Kept for workflow | `validate_benchmark_suite.mjs` and `validate_stability_result.mjs` now accept `--expectedChromiumRevision`; official, trusted, post-ATL long-stability, and artifact-audit paths pass the pinned checkout revision; `test_revision_validation.ps1` verifies stale benchmark and stability JSON are rejected | Low: validation-only; final evidence still requires real stock/fork binaries | `scripts/validate_benchmark_suite.mjs`, `scripts/validate_stability_result.mjs`, `scripts/run_official_comparison.ps1`, `scripts/run_trusted_experiment_matrix.ps1`, `scripts/run_post_atl_pipeline.ps1`, `scripts/audit_artifacts.ps1`, `scripts/test_revision_validation.ps1`, `docs/benchmark_methodology.md` | Prevents same-label result JSON from another Chromium revision from satisfying official performance or one-hour stability rows. |
| O-091 | Require external HTTP blocking in navigation-lock smoke evidence | Kept for workflow; runtime pending | `run_navigation_lock_tests.mjs` now attempts navigation to a reachable non-loopback local-interface HTTP server; `validate_smoke_result.mjs` requires `blocks_external_http_navigation`, verifies the attempted target is non-loopback, rejects pass-labelled results that land on that URL, and requires zero request hits on the blocked target; `test_smoke_detail_validation.ps1` covers valid, landed, request-hit, and loopback-target cases | Low: test/validation-only; final evidence still requires the patched fork binary and a host with a non-loopback IPv4 interface | `scripts/run_navigation_lock_tests.mjs`, `scripts/validate_smoke_result.mjs`, `scripts/test_smoke_detail_validation.ps1`, `scripts/test_smoke_coverage.ps1`, `docs/benchmark_methodology.md`, `docs/future_work.md` | Closes the pre-build test-coverage gap where HTTP navigation evidence only exercised loopback origins and avoids false passes from unreachable external URLs. |
| O-092 | Surface external-navigation host prerequisite in audit evidence | Kept for workflow | `write_environment_manifest.ps1` records `environment.navigation_external_ipv4_addresses` and a `navigation_external_ipv4` check; `audit_artifacts.ps1` adds a runtime-test prerequisite row from that check; `test_environment_manifest_coverage.ps1` verifies the manifest schema includes it | Low: manifest/audit-only; no browser runtime behavior changes | `scripts/write_environment_manifest.ps1`, `scripts/audit_artifacts.ps1`, `scripts/test_environment_manifest_coverage.ps1`, `docs/build.md`, `docs/benchmark_methodology.md` | Makes a missing non-loopback interface visible before running the post-ATL official comparison, where the fork navigation-lock smoke now depends on it. |
| O-093 | Fail prebuild verification on non-ATL manifest failures | Kept for workflow | `verify_prebuild.ps1` now validates `prebuild-environment.json` after writing it and permits only `visual_studio_atl` and `visual_studio_atl_component` failures under `-AllowMissingAtl`; `test_verify_prebuild_manifest_gate.ps1` extracts the actual verifier helper and proves clean manifests pass, ATL-only failures pass only with the allowance, and `navigation_external_ipv4` still fails under the ATL allowance | Low: verifier-only; no browser runtime behavior changes | `scripts/verify_prebuild.ps1`, `scripts/test_verify_prebuild_manifest_gate.ps1`, `scripts/audit_artifacts.ps1`, `docs/build.md` | Prevents a missing runtime prerequisite such as `navigation_external_ipv4` from being hidden by the temporary ATL-only bypass. |
| O-094 | Validate completed official manifest smoke artifacts semantically | Kept for workflow | `audit_artifacts.ps1` re-runs `validate_smoke_result.mjs` for completed official-manifest runtime, HTTP navigation-lock, and file-navigation-lock smoke files after their file/hash counts are present; `test_official_manifest_runtime_audit.ps1` verifies skipped, missing, invalid runtime, and invalid navigation smoke evidence are rejected | Low: audit-only; final evidence still requires real stock/fork smoke files from built binaries | `scripts/audit_artifacts.ps1`, `scripts/test_official_manifest_runtime_audit.ps1`, `docs/benchmark_methodology.md` | Prevents a completed manifest from passing final audit with hashed smoke JSON that fails the same semantic checks used when the official workflow writes the smoke files. |
| O-095 | Isolate synthetic completed manifest audit tests from real report paths | Kept for workflow | `audit_artifacts.ps1` supports test-only official/trusted manifest path overrides only when `THREE_BROWSER_ALLOW_TEST_MANIFEST_OVERRIDES=1` is set; the manifest-audit regression tests write synthetic completed manifests under `benchmarks/tmp/<test-name>/`; `test_audit_manifest_override_guard.ps1` proves ungated override env vars are rejected, and the focused manifest-audit suite passes and leaves no real or temp manifest artifacts behind | Low: test/audit-only; override variables are guarded and used only by regression tests | `scripts/audit_artifacts.ps1`, `scripts/test_audit_manifest_override_guard.ps1`, `scripts/test_manifest_package_audit.ps1`, `scripts/test_manifest_artifact_path_audit.ps1`, `scripts/test_official_manifest_*_audit.ps1`, `scripts/test_trusted_manifest_*_audit.ps1`, `docs/benchmark_methodology.md` | Prevents interrupted manifest-audit tests from leaving fake completed official or trusted evidence in `benchmarks/reports`, and prevents stray environment variables from redirecting real audits. |
| O-096 | Require official runtime smoke to prove stock/fork launch modes | Kept for workflow; runtime pending | `run_smoke_tests.mjs` now records `viewer_mode`, `viewer_trusted_content`, and `viewer_app_url`; `run_official_comparison.ps1` validates baseline smoke with `--expect-browser-mode` and launches fork smoke through `--viewer-app-url` plus navigation blocking and trusted-content switches; `validate_smoke_result.mjs` and `audit_artifacts.ps1` require `viewer_mode=false`/`viewer_trusted_content=false`/null viewer URL for `baseline-content-shell-runtime-smoke.json` and `viewer_mode=true`/`viewer_trusted_content=true`/valid viewer URL for `fork-viewer-default-runtime-smoke.json`; focused smoke and official-manifest runtime tests pass | Low: validation/launch workflow only; final evidence still requires stock and patched fork binaries | `scripts/run_smoke_tests.mjs`, `scripts/validate_smoke_result.mjs`, `scripts/run_official_comparison.ps1`, `scripts/audit_artifacts.ps1`, `scripts/test_smoke_coverage.ps1`, `scripts/test_smoke_detail_validation.ps1`, `scripts/test_official_manifest_runtime_audit.ps1`, `docs/benchmark_methodology.md` | Closes the gap where a completed official manifest could contain valid generic runtime smoke for the fork or viewer-mode smoke for stock without proving the exact stock-vs-viewer entrypoint split required by the runtime completion criteria. |
| O-097 | Bind official smoke artifacts to the manifest browser executables | Kept for workflow; runtime pending | `validate_smoke_result.mjs` accepts `--expected-browser` and compares it to `browser_executable`; `run_official_comparison.ps1` passes the baseline browser path for stock runtime smoke and the fork browser path for fork runtime plus HTTP/file navigation-lock smoke; `audit_artifacts.ps1` repeats those expected-browser checks from completed official manifests; focused smoke, dry-run, and manifest regression tests pass | Low: validation-only; final evidence still requires real stock/fork smoke files from built binaries | `scripts/validate_smoke_result.mjs`, `scripts/run_official_comparison.ps1`, `scripts/audit_artifacts.ps1`, `scripts/test_smoke_coverage.ps1`, `scripts/test_smoke_detail_validation.ps1`, `scripts/test_dry_run_manifests.ps1`, `scripts/test_official_manifest_runtime_audit.ps1`, `docs/benchmark_methodology.md` | Prevents a completed official manifest from satisfying runtime or navigation evidence with valid smoke JSON produced by installed Chrome, the wrong checkout binary, or the wrong side of the stock/fork comparison. |
| O-098 | Bind manifest metadata to declared artifact paths | Kept for workflow; runtime pending | `audit_artifacts.ps1` now compares each completed official/trusted file metadata path to the corresponding manifest-declared browser, build-args, result, runtime-smoke, navigation-lock, trace, and report path before accepting the hash; `test_manifest_artifact_path_audit.ps1` creates valid alternate files and verifies official and trusted manifests are rejected when metadata points to a different real file | Low: audit-only; adds path comparison on top of existing hash checks | `scripts/audit_artifacts.ps1`, `scripts/test_manifest_artifact_path_audit.ps1`, `docs/benchmark_methodology.md` | Prevents a completed manifest from listing one artifact path while supplying a valid SHA-256/size for another file. |
| O-099 | Bind package metadata to declared package directories | Kept for workflow; runtime pending | `audit_artifacts.ps1` now compares completed official/trusted package metadata paths to `package_dirs.baseline`, `package_dirs.fork`, or `package_dir` before accepting file count and size; `test_manifest_artifact_path_audit.ps1` creates alternate real package directories and verifies official and trusted manifests reject metadata for the wrong directory | Low: audit-only; adds path comparison on top of existing package count/size checks | `scripts/audit_artifacts.ps1`, `scripts/test_manifest_artifact_path_audit.ps1`, `docs/benchmark_methodology.md` | Prevents packaged benchmark evidence from naming one staged runtime directory while attaching valid metadata for another. |
| O-100 | Add benchmark switches to staged package launcher | Kept for workflow; runtime pending | `stage_viewer_package.ps1` now writes `run_viewer.ps1` with explicit `-Benchmark`, scene, renderer, duration, warmup, complexity, resource-warmup, GPU-timing, start-delay, and extra Chromium argument parameters; `test_stage_viewer_package.ps1` verifies those launcher fields with synthetic package inputs | Low: packaging/launch convenience only; official JSON capture still goes through `run_benchmark.mjs` | `scripts/stage_viewer_package.ps1`, `scripts/test_stage_viewer_package.ps1`, `docs/build.md`, `docs/benchmark_methodology.md` | Makes the staged fork package directly usable for deterministic local benchmark launches while preserving the CDP harness as the auditable measurement path. |
| O-101 | Mirror safe benchmark-harness launch switches in packaged launcher | Kept for workflow; runtime pending | `run_viewer.ps1` generated by `stage_viewer_package.ps1` now uses a package-local profile and includes the safe low-noise/WebGPU switches from `run_benchmark.mjs`, including disabled background services, WebGPU developer features, and background throttling disables; `test_stage_viewer_package.ps1` verifies those launcher switches | Low to medium: safe launch defaults affect packaged runtime behavior but avoid risky process/GPU experiments unless supplied through `-ExtraArgs` | `scripts/stage_viewer_package.ps1`, `scripts/test_stage_viewer_package.ps1`, `docs/benchmark_methodology.md` | Keeps packaged manual benchmark launches closer to official harness conditions and avoids using the user's default browser profile. |
| O-102 | Forward viewer benchmark-result marker to stdout | Draft patch kept; runtime pending | `chromium_patches/0001-draft-minimal-three-viewer-entrypoint.patch` updates `Shell::DidAddMessageToConsole` so viewer mode forwards only `THREE_VIEWER_RESULT` console messages to stdout and flushes them; `test_viewer_patch_stdout_result.ps1` verifies the marker gate, stdout write, and preserved default console behavior | Low: result-reporting path only; official JSON capture still uses CDP and runtime validation requires the fork binary | `chromium_patches/0001-draft-minimal-three-viewer-entrypoint.patch`, `scripts/test_viewer_patch_stdout_result.ps1`, `chromium_patches/minimal_viewer_entrypoint.md`, `docs/build.md`, `docs/benchmark_methodology.md` | Gives the Chromium-derived executable a direct packaged/manual benchmark result surface without forwarding arbitrary page console noise. |
| O-103 | Use a single-line benchmark result marker with backward-compatible CDP parsing | Kept for workflow; runtime pending | `viewer/src/main.js` now logs `THREE_VIEWER_RESULT <json>` as one console string so the stdout-forwarding path receives the full payload; `run_benchmark.mjs`, `run_smoke_tests.mjs`, and `run_trace_capture.mjs` parse both the single-string form and the older two-argument console form; focused runtime-surface, smoke-coverage, and trace-start-delay tests pass | Low: harness/result parsing only; official result JSON still requires real stock/fork runs | `viewer/src/main.js`, `scripts/run_benchmark.mjs`, `scripts/run_smoke_tests.mjs`, `scripts/run_trace_capture.mjs`, `scripts/test_viewer_runtime_surface.ps1`, `scripts/test_smoke_coverage.ps1`, `scripts/test_trace_capture_start_delay.ps1` | Makes packaged stdout output complete while preserving compatibility with existing CDP result collection. |
| O-104 | Refresh Chromium pin to upstream HEAD and read expected revision dynamically | Kept for workflow; build pending | `.chromium_revision` holds the active Chromium pin; `src` sync, hooks, patch applicability, prebuild verification with ATL allowance, artifact audit, and GN generation-only for baseline/default/trusted outputs have passed for the active pin; `audit_artifacts.ps1` reads the expected revision from `.chromium_revision` instead of embedding a SHA | Low to medium: revision bumps can invalidate patch assumptions and cached generated metadata; patch applicability and prebuild evidence must be rerun after each bump | `.chromium_revision`, `scripts/audit_artifacts.ps1`, `docs/build.md`, `docs/requirement_traceability.md`, `docs/completion_audit.md` | Keeps the fork-first work tied to current Chromium source while preserving the same-revision stock/fork evidence rule. ATL/MFC is still required before real builds and benchmarks can resume. |
| O-105 | Record upstream Chromium HEAD freshness in the prebuild manifest | Kept for workflow; build pending | `write_environment_manifest.ps1` now records `chromium.upstream_head_revision`, `chromium.upstream_head_checked_at`, and `chromium.upstream_head_matches_expected`; `audit_artifacts.ps1` includes that observation in the prebuild manifest evidence; `test_environment_manifest_coverage.ps1` verifies the new fields | Low: network-dependent observation only; it is intentionally not a hard gate because Chromium can move during long build and benchmark runs | `scripts/write_environment_manifest.ps1`, `scripts/test_environment_manifest_coverage.ps1`, `scripts/audit_artifacts.ps1`, `docs/build.md` | Future prebuild runs expose whether the active pin matched upstream HEAD when checked, while official stock/fork evidence remains reproducibly tied to `.chromium_revision`. |
| O-106 | Add focused manifest-audit mode for synthetic manifest regression tests | Kept for workflow; build pending | `audit_artifacts.ps1 -ManifestAuditOnly` runs only the official/trusted manifest rows, and the synthetic completed-manifest regression tests now use that mode; focused manifest tests pass in seconds while preserving the same manifest validation assertions | Low: test/runtime workflow only; the full artifact audit still runs every checklist row for normal completion evidence | `scripts/audit_artifacts.ps1`, `scripts/test_manifest_artifact_path_audit.ps1`, `scripts/test_manifest_package_audit.ps1`, `scripts/test_official_manifest_*_audit.ps1`, `scripts/test_trusted_manifest_*_audit.ps1` | Keeps `verify_prebuild.ps1` comprehensive without repeatedly running unrelated checklist sections inside each synthetic manifest test. |
| O-107 | Add a reproducible Chromium pin refresh helper | Kept for workflow; build pending | `refresh_chromium_pin.ps1` can fetch the current upstream Chromium HEAD or a supplied revision, update `.chromium_revision`, run the Chromium sync, regenerate baseline/default/trusted GN metadata, rewrite the prebuild environment manifest, and check viewer patch applicability; `test_refresh_chromium_pin_dry_run.ps1` verifies the command graph is non-mutating under `-DryRun` | Low to medium: the real refresh mutates the Chromium checkout and pin, so it refuses dirty `src` state and the dry-run is what runs in prebuild verification | `scripts/refresh_chromium_pin.ps1`, `scripts/test_refresh_chromium_pin_dry_run.ps1`, `scripts/verify_prebuild.ps1`, `docs/build.md`, `docs/rebase_strategy.md` | Makes the "current Chromium source" refresh reproducible before starting a stock/fork build cycle instead of relying on manual `git fetch`, checkout, sync, and GN regeneration steps. |
| O-108 | Let the post-ATL pipeline refresh the Chromium pin | Kept for workflow; build pending | `run_post_atl_pipeline.ps1 -RefreshChromiumPin` now invokes the pin refresh helper before verification/build/benchmark steps and recomputes the Chromium and patch-derived fork revisions afterward; `-RefreshRevision <sha>` targets a specific revision, and dry-run output carries the planned revision through trusted-matrix and long-stability handoffs | Low to medium: real refresh still mutates `src` and `.chromium_revision`, so it remains explicit and uses the refresh helper's clean-checkout guard | `scripts/run_post_atl_pipeline.ps1`, `scripts/test_post_atl_pipeline_dry_run.ps1`, `scripts/install_vs_atl.ps1`, `scripts/check_prereqs.ps1`, `README.md`, `docs/build.md`, `docs/benchmark_methodology.md` | Reduces the post-ATL recovery path to one command while keeping same-revision stock/fork evidence tied to the final refreshed checkout. |
| O-109 | Require trusted viewer mode for unsafe experiment flags | Kept for workflow; build pending | The draft patch now activates unsafe native aliases only when both `--viewer-app-url` and `--viewer-trusted-content` are present; `run_benchmark.mjs` and `run_trace_capture.mjs` reject unsafe viewer experiment flags unless both `--viewerMode` and `--viewerTrustedContent` are requested; focused gate tests exercise both static patch coverage and no-browser dynamic harness failures | Low: stricter validation can reject ad hoc local invocations that previously produced misleading metadata; runtime behavior still requires fork binary validation | `chromium_patches/0001-draft-minimal-three-viewer-entrypoint.patch`, `scripts/run_benchmark.mjs`, `scripts/run_trace_capture.mjs`, `scripts/test_viewer_patch_trusted_gates.ps1`, `scripts/test_benchmark_flag_metadata.ps1`, `docs/trusted_content_flags.md` | Prevents trusted-only process/GPU aliases from being applied to non-viewer launches and prevents benchmark artifacts from claiming aggressive viewer flags that the fork would ignore outside trusted viewer mode. |
| O-110 | Stamp navigation-lock launch metadata in benchmark JSON | Kept for workflow; build pending | `run_benchmark.mjs` now records `viewer_block_external_navigation` as true whenever viewer mode launches with `--viewer-block-external-navigation`; official and trusted suite validations include expected metadata for fork viewer runs, and focused flag-metadata tests cover the field | Low: metadata/validation-only; runtime navigation blocking still requires fork HTTP/file navigation-lock smoke after the binary exists | `scripts/run_benchmark.mjs`, `scripts/validate_metrics.mjs`, `scripts/run_official_comparison.ps1`, `scripts/run_trusted_experiment_matrix.ps1`, `scripts/test_benchmark_flag_metadata.ps1`, `scripts/test_benchmark_suite_flag_metadata.ps1`, `scripts/test_dry_run_manifests.ps1`, `docs/benchmark_methodology.md` | Prevents a fork benchmark suite from satisfying viewer-mode evidence if the per-scene JSON does not show that navigation-lock launch mode was requested. |
| O-111 | Enforce viewer launch metadata for one-hour stability | Kept for workflow; build pending | `validate_stability_result.mjs` now accepts repeated `--expectedFlagMetadata key=value`; `run_long_stability.ps1` passes expected viewer-mode, trusted-content, navigation-lock, and aggressive-flag metadata for each long run; `audit_artifacts.ps1` requires browser-mode metadata for stock stability and trusted viewer/navigation-lock metadata for fork stability | Low: validation-only; old long-stability JSON without the new metadata is intentionally rejected as insufficient final evidence | `scripts/validate_stability_result.mjs`, `scripts/run_long_stability.ps1`, `scripts/audit_artifacts.ps1`, `scripts/test_stability_result_thresholds.ps1`, `docs/stability_behavior.md`, `docs/benchmark_methodology.md` | Prevents a one-hour fork stability file from satisfying completion if it was not launched through the viewer-mode trusted local navigation-locked path. |
| O-112 | Repeat viewer flag gates in final raw-suite audit rows | Kept for workflow; build pending | `audit_artifacts.ps1` now passes `--expectedFlagMetadata` into direct official performance rows: stock WebGL2/WebGPU rows require browser-mode metadata, fork default rows require trusted viewer/navigation-lock metadata, and aggressive rows require common aggressive viewer metadata; `test_audit_official_suite_validation_gates.ps1` verifies those static audit row gates | Low: audit-only; completed official manifests already require matching metadata, but this prevents loose raw JSON globs from becoming proxy evidence | `scripts/audit_artifacts.ps1`, `scripts/test_audit_official_suite_validation_gates.ps1`, `scripts/validate_benchmark_suite.mjs`, `docs/benchmark_methodology.md` | Keeps the generated prompt-to-artifact checklist aligned with the official/trusted suite validators instead of relying on completed manifests alone for viewer-launch metadata. |
| O-113 | Mark stale Siso failure logs after GN regeneration | Kept for workflow; build pending | `write_environment_manifest.ps1` now records Siso failure log timestamps, GN output timestamps, and freshness booleans; `audit_artifacts.ps1` only labels non-stale failure logs as the latest build failure and reports older logs as prior evidence; `test_environment_manifest_coverage.ps1` verifies both real manifest fields and synthetic stale/fresh failure detection | Low: manifest/audit-only; it changes evidence wording but not build behavior | `scripts/write_environment_manifest.ps1`, `scripts/audit_artifacts.ps1`, `scripts/test_environment_manifest_coverage.ps1`, `docs/build.md` | Prevents a post-refresh checklist from treating a pre-refresh `siso_output` file as the latest build attempt after `gn gen` has regenerated the output directory. |
| O-114 | Surface upstream Chromium HEAD freshness as a build audit row | Kept for workflow; build pending | `audit_artifacts.ps1` now adds an explicit `Observed upstream Chromium HEAD freshness` build row from the selected prebuild environment manifest; the row is done only when the active pin matches the observed upstream HEAD and pending otherwise; `test_audit_fail_on_incomplete.ps1` verifies the row is present in generated checklists, and `test_upstream_freshness_audit.ps1` verifies matching and stale temp manifests through `-EnvironmentManifest` and `-BuildStateOnly` | Low: audit-only; upstream can move during long runs, so this is a freshness signal and the same-revision build/benchmark gates still rely on the fixed pin | `scripts/audit_artifacts.ps1`, `scripts/test_audit_fail_on_incomplete.ps1`, `scripts/test_upstream_freshness_audit.ps1`, `benchmarks/reports/prebuild-environment.json`, `docs/prompt_to_artifact_checklist.md` | Prevents the prompt-to-artifact checklist from satisfying "current Chromium source" with only a reproducible but stale pin. The post-ATL workflow still refreshes the pin immediately before final stock/fork builds, and focused audit modes now honor the manifest path they are passed. |
| O-115 | Verify harness-owned benchmark output path | Kept for workflow; build pending | `test_viewer_runtime_surface.ps1` now verifies the runner keeps the `--output` CLI/default option, creates the output directory, and writes the final JSON through `fs.writeFileSync(path.resolve(args.output), ...)`; `benchmark_methodology.md` documents that the viewer page emits only the `THREE_VIEWER_RESULT <json>` marker while the CDP harness owns JSON enrichment and persistence | Low: static workflow check only; final machine-readable evidence still requires real stock/fork benchmark runs | `scripts/test_viewer_runtime_surface.ps1`, `scripts/run_benchmark.mjs`, `viewer/src/main.js`, `docs/benchmark_methodology.md` | Prevents the benchmark-output requirement from being misread as browser-page filesystem access and guards against losing the harness-side JSON write path. |
| O-116 | Set a default RSS growth threshold for one-hour stability | Kept for workflow; build pending | The completion-oriented post-ATL command, stability docs, and artifact audit now use `-MaxRssDeltaMb 128` together with `-MaxRendererResourceDelta 0`; `audit_artifacts.ps1` passes both thresholds into `validate_stability_result.mjs`, and `test_stability_result_thresholds.ps1` verifies the validator rejects RSS growth over 128 MB and that the audit is wired to the default thresholds | Low to medium: the 128 MB threshold may need adjustment if real hardware/driver process accounting shows benign variance, but any relaxation must be documented | `scripts/audit_artifacts.ps1`, `scripts/test_stability_result_thresholds.ps1`, `scripts/run_long_stability.ps1`, `scripts/run_post_atl_pipeline.ps1`, `docs/stability_behavior.md`, `docs/benchmark_methodology.md`, `docs/build.md` | Turns the prompt's "agreed threshold" stability requirement into a concrete default gate before stock/fork one-hour evidence is produced. |
| O-117 | Require non-empty binary artifacts in the final checklist | Kept for workflow; build pending | `audit_artifacts.ps1` now uses a dedicated binary row helper for stock and fork `content_shell.exe` evidence, requiring a non-empty file and recording SHA-256; `test_binary_audit_rows.ps1` verifies those rows do not regress to existence-only checks, and `verify_prebuild.ps1` runs the test | Low: audit-only; real build provenance still comes from official runtime smoke, benchmark JSON, and completed manifests | `scripts/audit_artifacts.ps1`, `scripts/test_binary_audit_rows.ps1`, `scripts/verify_prebuild.ps1`, `docs/benchmark_methodology.md` | Prevents placeholder executables from satisfying the build artifact rows before stock/fork runtime and benchmark evidence exists. |
| O-118 | Align long-stability script defaults with the final stability gate | Kept for workflow; build pending | `run_long_stability.ps1` and `run_post_atl_pipeline.ps1` now default to `MaxRssDeltaMb=128` and `MaxRendererResourceDelta=0`; `test_stability_result_thresholds.ps1` statically verifies those defaults in addition to validator and artifact-audit threshold wiring | Low to medium: callers running deliberately growing resource-stress scenes must override thresholds explicitly and document why | `scripts/run_long_stability.ps1`, `scripts/run_post_atl_pipeline.ps1`, `scripts/test_stability_result_thresholds.ps1`, `docs/build.md`, `docs/benchmark_methodology.md` | Ensures the default post-ATL long-stability path cannot produce weaker evidence than the final prompt-to-artifact audit requires. |
| O-119 | Validate source-investigation paths after Chromium refreshes | Kept for workflow; build pending | `test_source_investigation_paths.ps1` parses `docs/source_investigation.md`, verifies the documented last-verification revision matches `.chromium_revision` and `src` HEAD, resolves documented Chromium `src/...` paths, and spot-checks sentinel symbols for content shell, WebGL validation, WebGPU/Dawn, Viz aggregation, and GPU process launch; `verify_prebuild.ps1` and `audit_artifacts.ps1` now run and report the test | Low: static source-map validation only; it does not prove an optimization works or close any measured optimization class | `scripts/test_source_investigation_paths.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `docs/source_investigation.md` | Prevents stale source-path or stale revision documentation from surviving a Chromium pin refresh before post-ATL traces and source-level experiments begin. |
| O-120 | Record upstream-head pin refresh provenance | Kept for workflow; build pending | `refresh_chromium_pin.ps1` now writes `benchmarks/reports/chromium-pin-refresh.json` when it selects a target revision from upstream HEAD; `write_environment_manifest.ps1` embeds that provenance, and `audit_artifacts.ps1` accepts it when a later upstream probe fails or observes drift; `test_upstream_freshness_audit.ps1` covers fresh, stale, drift-after-refresh, and unobserved-after-refresh cases | Low: audit/provenance-only; it must not be used to compare stock and fork binaries from different revisions | `scripts/refresh_chromium_pin.ps1`, `scripts/write_environment_manifest.ps1`, `scripts/audit_artifacts.ps1`, `scripts/test_upstream_freshness_audit.ps1`, `benchmarks/reports/chromium-pin-refresh.json`, `docs/build.md`, `docs/benchmark_methodology.md` | Prevents `-FinalGate` from failing merely because Chromium moves or the network probe fails during long post-ATL build, benchmark, and stability runs after a legitimate upstream-head refresh. |
| O-121 | Expose Chromium pin refresh provenance as a checklist row | Kept for workflow; build pending | `audit_artifacts.ps1` now adds a direct `Chromium pin refresh provenance` row, requiring the embedded refresh artifact to target the active pin and report `selected_from_upstream_head=true`; `test_upstream_freshness_audit.ps1` verifies provenance-present and provenance-missing row behavior, and `test_audit_fail_on_incomplete.ps1` verifies the row remains part of the final checklist | Low: audit-only; it makes current-source provenance visible but does not replace stock/fork same-revision benchmark evidence | `scripts/audit_artifacts.ps1`, `scripts/test_upstream_freshness_audit.ps1`, `scripts/test_audit_fail_on_incomplete.ps1`, `benchmarks/reports/chromium-pin-refresh.json` | Makes the "current Chromium source" evidence auditable as its own artifact rather than only through the upstream freshness row. |
| O-122 | Validate removed-subsystem register structure | Kept for workflow; build pending | `test_removed_subsystems_register.ps1` validates that removal/disable candidate tables in `docs/removed_subsystems.md` include non-empty rationale, regression risk, and current evidence/source cells, and that the document keeps the no-physical-removal and measured-evidence guardrails; `verify_prebuild.ps1` and `audit_artifacts.ps1` now run and report the test | Low: documentation-structure only; it does not convert pending subsystem removals into measured outcomes | `scripts/test_removed_subsystems_register.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `docs/removed_subsystems.md` | Prevents the "every removed subsystem must be documented with rationale and regression risk" requirement from degrading into unchecked prose before post-ATL measured removals begin. |
| O-123 | Validate trusted-content flag documentation structure | Kept for workflow; build pending | `test_trusted_content_flags_doc.ps1` validates that `docs/trusted_content_flags.md` documents all viewer switches, risk classifications, trusted-content guardrail text, pass-through decoder aliasing, reserved no-op gate status, and raw benchmark metadata fields; `verify_prebuild.ps1` and `audit_artifacts.ps1` now run and report the test | Low: documentation-structure only; runtime gating is still covered by patch and harness tests, and measured evidence still requires the fork binary | `scripts/test_trusted_content_flags_doc.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `docs/trusted_content_flags.md` | Keeps the unsafe optimization flag documentation aligned with the prompt requirement that aggressive modes are explicit, trusted-content-gated, risk-documented experiments. |
| O-124 | Validate required scene-suite coverage consistency | Kept for workflow; build pending | `test_scene_coverage_consistency.ps1` verifies the exact required scene sequence across `run_full_suite.ps1`, `run_official_comparison.ps1`, `run_trusted_experiment_matrix.ps1`, `validate_metrics.mjs`, `validate_benchmark_suite.mjs`, `audit_artifacts.ps1`, `viewer/src/scenes.js`, `docs/benchmark_methodology.md`, and `docs/webgpu_scene_coverage.md`; `verify_prebuild.ps1` and `audit_artifacts.ps1` now run and report the test | Low: static coverage guard only; final scene performance evidence still requires same-revision stock/fork benchmark runs | `scripts/test_scene_coverage_consistency.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `viewer/src/scenes.js`, `docs/benchmark_methodology.md`, `docs/webgpu_scene_coverage.md` | Prevents a future scene addition/removal from silently drifting across viewer implementation, official/trusted runners, validators, audit requirements, and WebGPU coverage documentation. |
| O-125 | Validate required documentation structure | Kept for workflow; build pending | `test_documentation_structure.ps1` verifies the required documentation corpus contains top-level headings plus the core objective, blocker, reproduction, benchmark, stability, trusted-flag, source-map, completion-audit, and same-revision evidence terms; `verify_prebuild.ps1` and `audit_artifacts.ps1` now run and report the test | Low: documentation-structure only; final docs still need measured stock/fork results and optimization outcomes after the build blocker is removed | `scripts/test_documentation_structure.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `README.md`, `docs/*.md` | Prevents the required documentation set from degrading into empty files or losing the ATL handoff, post-ATL pipeline, same-revision caveats, or completion-audit structure while runtime evidence is still pending. |
| O-126 | Validate GN args profile policy | Kept for workflow; build pending | `test_gn_args_profiles.ps1` verifies the baseline, fork default, and trusted/aggressive GN templates are release-mode, non-component, comparable profiles with the expected symbol settings and that unsafe viewer/runtime experiment terms are not baked into GN args; `audit_artifacts.ps1` reports this as a build row and `verify_prebuild.ps1` runs it | Low: build-input guard only; it does not replace real stock/fork builds or benchmark evidence | `build/gn_args/*.gn`, `scripts/test_gn_args_profiles.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1` | Keeps the same-revision comparison fair by preventing silent GN-template drift and ensuring the trusted/aggressive profile remains metadata until explicit runtime flags are measured. |
| O-127 | Validate patch notes against the draft patch | Kept for workflow; build pending | `test_patch_notes_consistency.ps1` verifies the draft patch's touched Chromium files, viewer switches, behavior notes, pending stdout-startup limitation, and patch policy are reflected in `chromium_patches/minimal_viewer_entrypoint.md`, `chromium_patches/README.md`, and `docs/trusted_content_flags.md`; `verify_prebuild.ps1` and `audit_artifacts.ps1` now run and report it | Low: documentation/patch-note guard only; it does not replace patch application, build, or runtime smoke evidence | `chromium_patches/0001-draft-minimal-three-viewer-entrypoint.patch`, `chromium_patches/minimal_viewer_entrypoint.md`, `chromium_patches/README.md`, `docs/trusted_content_flags.md`, `scripts/test_patch_notes_consistency.ps1` | Prevents patch notes from drifting away from the actual fork patch while baseline/fork binaries and runtime evidence are still pending. |
| O-128 | Validate benchmark metadata enrichment path | Kept for workflow; build pending | `test_benchmark_metadata_enrichment.ps1` verifies `run_benchmark.mjs` enriches viewer results with SHA-256 build-args provenance, pinned checkout revision, fork revision, checkout-built flag, GPU/backend metadata, JS heap, process-tree RSS start/peak/end/delta, browser binary size, viewer bundle size, package size, schema completion, and harness-owned JSON output writing; `verify_prebuild.ps1` and `audit_artifacts.ps1` now run and report it | Low: static runner regression only; official values still require real stock/fork browser executions | `scripts/run_benchmark.mjs`, `scripts/validate_metrics.mjs`, `scripts/test_benchmark_metadata_enrichment.ps1`, `docs/benchmark_methodology.md` | Prevents future runner edits from silently dropping host-side provenance or artifact metadata needed to make same-revision performance claims auditable. |
| O-129 | Expand human-readable report metric coverage | Kept for workflow; build pending | `summarize_results.mjs` and `compare_results.mjs` now include the core FPS, low-FPS, percentile/max frame time, CPU/GPU/JS/submission/compositor/presentation, dropped-frame, draw-call, triangle, upload, shader-event, memory, startup, binary, viewer bundle, and package-size metrics; `test_report_metric_coverage.ps1` verifies the report scripts keep those field references and table headers | Low: reporting-only change; raw JSON remains the source of truth and official reports still require stock/fork binaries | `scripts/summarize_results.mjs`, `scripts/compare_results.mjs`, `scripts/test_report_metric_coverage.ps1`, `docs/benchmark_methodology.md` | Prevents the human-readable benchmark reports from shrinking back to a small FPS-only view while the machine-readable schema continues collecting broader evidence. |
| O-130 | Wire relaxed WebGL validation gate to pass-through command decoder | Draft patch kept; build pending | `--viewer-relaxed-webgl-validation` now maps to Chromium's existing `--use-cmd-decoder=passthrough` only inside the trusted-content configuration path, which itself requires both `--viewer-app-url` and `--viewer-trusted-content`; static patch and trusted-flag documentation tests cover the alias and leave the Blink/direct-presentation gates as no-ops | High: pass-through command decoder changes WebGL command validation and driver exposure behavior; it must stay trusted-only and must be benchmarked and smoke-tested against stock and fork default before any keep/revert decision | `chromium_patches/0001-draft-minimal-three-viewer-entrypoint.patch`, `scripts/test_viewer_patch_trusted_gates.ps1`, `scripts/test_trusted_content_flags_doc.ps1`, `docs/trusted_content_flags.md`, `docs/removed_subsystems.md` | Converts one requested aggressive trusted-content experiment from a placeholder gate into a concrete existing-Chromium runtime switch, without enabling it by default or baking it into GN args. |
| O-131 | Align trace capture with trusted experiment flags | Kept for workflow; build pending | `run_trace_capture.mjs` now accepts the same trusted viewer experiment switches as the benchmark wrapper, applies the same `--viewerMode` plus `--viewerTrustedContent` gate, forwards those switches to the browser, and records requested flag metadata in the trace sidecar; `test_benchmark_flag_metadata.ps1` verifies trace parser support, switch emission, sidecar fields, and unsafe-flag rejection | Low: trace-workflow change only; raw trace capture still requires stock/fork binaries and tracing can perturb timings | `scripts/run_trace_capture.mjs`, `scripts/test_benchmark_flag_metadata.ps1`, `docs/benchmark_methodology.md`, `docs/requirement_traceability.md` | Lets post-build trace analysis inspect the same aggressive experiments that the benchmark and trusted-matrix workflows measure, instead of only the aggressive GPU bundle. |
| O-132 | Validate trace sidecar launch metadata | Kept for workflow; build pending | `validate_trace_result.mjs` now accepts `--expectedBrowser` plus repeated `--expectedFlagMetadata key=value` checks; artifact audit passes the manifest baseline/fork browser paths, browser-mode expectations for stock trace sidecars, and trusted viewer/navigation-lock expectations for fork trace sidecars; focused trace-result and official-manifest trace tests verify mismatched browser and flag metadata are rejected | Low: validation-only; it strengthens trace provenance without changing browser runtime behavior | `scripts/validate_trace_result.mjs`, `scripts/audit_artifacts.ps1`, `scripts/test_trace_result_validation.ps1`, `scripts/test_official_manifest_trace_audit.ps1`, `docs/benchmark_methodology.md`, `docs/requirement_traceability.md` | Prevents a completed trace manifest from satisfying diagnostic trace evidence with stock/fork sidecars captured from the wrong executable, the wrong launch mode, or without the viewer navigation-lock path. |
| O-133 | Validate official traces immediately after capture | Kept for workflow; build pending | `run_official_comparison.ps1 -CaptureTrace` now runs `validate_trace_file.mjs --minEvents 1` and `validate_trace_result.mjs` after each stock/fork trace capture, checking expected browser, scene, renderer, duration, warmup, start delay, and stock/fork launch flag metadata before trace summaries are generated; `test_dry_run_manifests.ps1` verifies those commands appear in the official dry-run graph | Low: validation-only; it can only catch provenance and trace-shape errors once real stock/fork binaries produce trace artifacts | `scripts/run_official_comparison.ps1`, `scripts/test_dry_run_manifests.ps1`, `docs/benchmark_methodology.md` | Moves trace provenance failure earlier in the post-ATL run instead of relying only on the final artifact audit to reject bad trace evidence. |
| O-134 | Include ANGLE backend experiment in the completion handoff | Kept for workflow; build pending | `run_post_atl_pipeline.ps1` now always supplies a trusted-matrix ANGLE experiment when that matrix runs: explicit `-TrustedMatrixAngleBackend` values win, otherwise `-AggressiveAngleBackend` is reused, otherwise it falls back to `d3d11`; README/build/methodology handoff commands pass `-TrustedMatrixAngleBackend d3d11` explicitly, and `test_post_atl_pipeline_dry_run.ps1` verifies explicit D3D11, implicit D3D11, and fallback to a non-D3D11 aggressive backend | Low: orchestration-only; D3D11 is the Windows baseline ANGLE experiment and additional backends still need explicit measured runs where supported | `scripts/run_post_atl_pipeline.ps1`, `scripts/test_post_atl_pipeline_dry_run.ps1`, `README.md`, `docs/build.md`, `docs/benchmark_methodology.md`, `docs/requirement_traceability.md`, `docs/completion_audit.md` | Prevents the one-command post-ATL workflow from skipping the prompt-required ANGLE backend evaluation path. |
| O-135 | Tighten top-level reproduction handoff audit | Kept for workflow; build pending | `audit_artifacts.ps1` now requires the README handoff to include the full completion command shape: Chromium pin refresh, WebGPU, aggressive GPU, trace capture, trusted matrix, in-process GPU, single-process, D3D11 ANGLE, reserved gates, one-hour stability, RSS/resource thresholds, and final gate; `test_documentation_structure.ps1` requires the same high-risk terms in README/build docs | Low: documentation/audit-only; it prevents stale operator instructions but cannot replace the actual post-ATL build and benchmark run | `scripts/audit_artifacts.ps1`, `scripts/test_documentation_structure.ps1`, `README.md`, `docs/build.md` | Prevents the prompt-to-artifact checklist from marking the reproduction handoff done if the top-level command drops required benchmark, trace, trusted experiment, stability, or final-audit switches. |
| O-136 | Regression-test top-level reproduction handoff terms | Kept for workflow; build pending | `test_reproduction_handoff_audit.ps1` verifies the README, build document, artifact audit, and documentation structure test all require the full post-ATL command terms, including trace capture, trusted sub-experiments, D3D11 ANGLE, RSS/resource thresholds, and final gate; `verify_prebuild.ps1` and `audit_artifacts.ps1` now run/report this focused check | Low: test-only; it protects operator handoff text but still cannot satisfy build, runtime, or performance evidence rows without ATL and real binaries | `scripts/test_reproduction_handoff_audit.ps1`, `scripts/verify_prebuild.ps1`, `scripts/audit_artifacts.ps1`, `README.md`, `docs/build.md` | Adds a focused guard for the reproduction command instead of relying on broad documentation structure checks. |
| O-137 | Stage baseline and fork packages in the post-ATL evidence path | Kept for workflow; build pending | `run_post_atl_pipeline.ps1` stages `baseline-content-shell` and `viewer-default` packages, passes both into `run_official_comparison.ps1`, passes the matching package directories into stock/fork long-stability, and the dry-run tests verify package staging, manifest metadata, and suite-validation handoffs | Low: packaging/orchestration-only; the baseline package launcher is not the primary stock runtime path, and real `package_size_mb` evidence still needs stock/fork binaries | `scripts/run_post_atl_pipeline.ps1`, `scripts/run_official_comparison.ps1`, `scripts/test_post_atl_pipeline_dry_run.ps1`, `scripts/test_dry_run_manifests.ps1`, `docs/build.md`, `docs/benchmark_methodology.md`, `docs/requirement_traceability.md`, `docs/completion_audit.md` | Makes package-size evidence symmetric between stock and fork official comparisons instead of recording it only for the fork package. |
| O-138 | Make optimization-class completion evidence-driven | Kept for workflow; build pending | `audit_artifacts.ps1` now reads the `Prompt Optimization Class Decisions` table below and keeps each prompt-required optimization class pending until it has a final status, measured effect, risk, evidence, and notes; `test_optimization_decision_audit.ps1` verifies pending rows are rejected and synthetic completed rows are accepted in optimization-only audit mode | Low: audit-only; it does not replace real benchmark evidence and only prevents the final checklist from being permanently pending or prematurely complete | `scripts/audit_artifacts.ps1`, `scripts/test_optimization_decision_audit.ps1`, `scripts/verify_prebuild.ps1`, `docs/optimization_log.md` | Converts optimization-class checklist rows from unconditional pending placeholders into rows that can close after same-revision measured decisions are recorded. |
| O-139 | Require complete official comparison options in final audit | Kept for workflow; build pending | `audit_artifacts.ps1` now adds an `Official comparison required options` row that remains pending unless the official manifest records WebGPU, aggressive GPU, an explicit aggressive ANGLE backend, trace capture, and baseline/fork package directories; the completed-manifest row also checks that aggressive labels, expected flag metadata, and result filenames match the requested backend; `test_official_manifest_required_options_audit.ps1` verifies all-missing, backend-only-missing, and complete synthetic manifests, and `test_official_manifest_aggressive_audit.ps1` verifies backend-label mismatch rejection | Low: audit-only; it raises the final evidence bar but does not change benchmark execution | `scripts/audit_artifacts.ps1`, `scripts/test_official_manifest_required_options_audit.ps1`, `scripts/test_official_manifest_aggressive_audit.ps1`, `scripts/verify_prebuild.ps1`, `docs/benchmark_methodology.md`, `docs/completion_audit.md`, `docs/requirement_traceability.md` | Prevents a completed WebGL2-only, default-backend aggressive, mislabeled-backend, non-traced, or unpackaged official manifest from satisfying the completion-oriented stock/fork evidence path. |
| O-140 | Require package-size evidence for one-hour stability | Kept for workflow; build pending | `validate_stability_result.mjs` now supports `--requirePackageSize`; `run_long_stability.ps1` enables it whenever `-PackageDir` is supplied; `audit_artifacts.ps1` requires it for both final stock and fork one-hour stability rows; `test_stability_result_thresholds.ps1` verifies positive, missing, and zero package-size behavior | Low: validation/orchestration-only; real one-hour evidence still needs stock/fork binaries and staged package directories | `scripts/validate_stability_result.mjs`, `scripts/run_long_stability.ps1`, `scripts/audit_artifacts.ps1`, `scripts/test_stability_result_thresholds.ps1`, `docs/benchmark_methodology.md`, `docs/requirement_traceability.md`, `docs/completion_audit.md` | Prevents a long-stability result from satisfying final evidence without the package-size metric required by the benchmark schema and post-ATL packaged run path. |
| O-141 | Require packaged trusted experiment matrix evidence | Kept for workflow; build pending | `audit_artifacts.ps1` now keeps completed trusted experiment matrix manifests pending unless they include a non-empty `package_dir` with matching package metadata; `test_trusted_manifest_required_options_audit.ps1` verifies missing-package and complete-package synthetic manifests, and the artifact-path fixture now supplies package metadata when testing unrelated path/hash failures | Low: audit-only; it raises the final evidence bar without changing browser launch behavior | `scripts/audit_artifacts.ps1`, `scripts/test_trusted_manifest_required_options_audit.ps1`, `scripts/test_manifest_artifact_path_audit.ps1`, `scripts/verify_prebuild.ps1`, `docs/benchmark_methodology.md`, `docs/completion_audit.md`, `docs/requirement_traceability.md` | Prevents an unpackaged trusted experiment matrix from satisfying the final optimization-evidence row when the completion workflow requires package-size evidence for the aggressive trusted fork path. |
| O-142 | Bind completed manifests to the current Chromium pin refresh | Kept for workflow; build pending | `audit_artifacts.ps1` now rejects completed official and trusted manifests unless their `generated_at` is after the current `chromium-pin-refresh.json` selected the same Chromium revision from upstream HEAD; official and trusted provenance tests mutate `generated_at` to prove stale pre-refresh manifests are rejected | Low: audit-only; it requires benchmark evidence to be rerun after an explicit pin refresh and preserves same-revision stock/fork comparisons | `scripts/audit_artifacts.ps1`, `scripts/test_official_manifest_provenance_audit.ps1`, `scripts/test_trusted_manifest_provenance_audit.ps1`, `docs/benchmark_methodology.md`, `docs/completion_audit.md`, `docs/requirement_traceability.md` | Prevents stock/fork or trusted benchmark manifests produced before the latest current-source refresh from satisfying final evidence after the pin is refreshed. |
| O-143 | Bind one-hour stability results to the Chromium pin refresh | Kept for workflow; build pending | `run_benchmark.mjs` now stamps benchmark JSON with `generated_at`; `validate_stability_result.mjs --pinRefreshManifest` rejects results generated before the current upstream-head pin refresh; `run_long_stability.ps1` and `audit_artifacts.ps1` pass the pin-refresh manifest for final checkout-built one-hour stability validation; `test_stability_result_thresholds.ps1` verifies stale generated-at rejection | Low: validation-only; old installed-Chrome smoke remains valid as harness evidence, but final stock/fork stability results must be regenerated by the current runner | `scripts/run_benchmark.mjs`, `scripts/validate_stability_result.mjs`, `scripts/run_long_stability.ps1`, `scripts/audit_artifacts.ps1`, `scripts/test_stability_result_thresholds.ps1`, `docs/benchmark_methodology.md`, `docs/completion_audit.md`, `docs/requirement_traceability.md` | Prevents stale raw one-hour stability JSON from satisfying final stability completion after a Chromium pin refresh. |
| O-144 | Record requested ANGLE backend for viewer-force aliases | Kept for workflow; build pending | `run_benchmark.mjs` now records `requested_angle_backend` from either direct `--angleBackend` or trusted `--viewerForceAngleBackend`, and falls back `angle_backend` to that requested value when renderer/CDP metadata does not populate it; official/trusted/stability expected flag metadata include the requested backend, and manifest audits reject aggressive official evidence missing that backend metadata | Low: metadata-only; it improves provenance for ANGLE experiments without changing launch flags | `scripts/run_benchmark.mjs`, `scripts/run_official_comparison.ps1`, `scripts/run_trusted_experiment_matrix.ps1`, `scripts/run_long_stability.ps1`, `scripts/audit_artifacts.ps1`, `docs/benchmark_methodology.md`, `docs/trusted_content_flags.md` | Prevents aggressive or trusted ANGLE runs from producing raw JSON that only records the viewer alias while leaving the normalized requested backend ambiguous. |
| O-145 | Include aggressive WebGPU in official comparison evidence | Kept for workflow; build pending | `run_official_comparison.ps1` now runs and validates a `fork-viewer-aggressive-gpu-<backend>-webgpu` suite whenever WebGPU and aggressive GPU are both requested, includes those files in the official WebGPU comparison report, and records `aggressive_webgpu` result and metadata entries in the manifest; `audit_artifacts.ps1` rejects completed manifests missing aggressive WebGPU result hashes or expected flag metadata; dry-run and aggressive-manifest tests verify the command and audit shape | Low: orchestration/audit-only until the fork binary exists; it increases benchmark time but closes the WebGPU aggressive comparison gap | `scripts/run_official_comparison.ps1`, `scripts/audit_artifacts.ps1`, `scripts/test_dry_run_manifests.ps1`, `scripts/test_official_manifest_aggressive_audit.ps1`, `docs/benchmark_methodology.md`, `docs/requirement_traceability.md` | Prevents a completed WebGPU official comparison from covering only stock/default while the trusted aggressive profile is measured only on WebGL2. |
| O-146 | Tighten strict official fork-revision provenance | Kept for workflow; build pending | `compare_results.mjs --strictOfficial` now rejects fork results whose `fork_revision` does not start with `<chromium_revision>+viewerpatch-` and rejects stock/baseline rows that carry fork revision metadata; `test_strict_official_rejects_software_rendering.ps1` covers missing, mismatched, non-viewerpatch, and baseline fork-revision failures | Low: report-validation-only; real official manifests already require exact patch-derived fork revisions through suite validation and artifact audit | `scripts/compare_results.mjs`, `scripts/test_strict_official_rejects_software_rendering.ps1`, `docs/benchmark_methodology.md`, `docs/requirement_traceability.md` | Prevents strict official reports from accepting fork-like provenance that only shares the Chromium SHA or from treating baseline rows as fork-derived artifacts. |
| O-147 | Validate exact official manifest suites before completion | Kept for workflow; build pending | `audit_artifacts.ps1` now re-runs `validate_benchmark_suite.mjs` against every exact stock/fork/default/aggressive result-file list declared by a completed official manifest after path/hash checks pass; it carries package-size requirements from manifest package dirs into both direct suite rows and manifest validation; `test_official_manifest_suite_semantics_audit.ps1` proves a manifest with matching paths/hashes is still rejected when a result file has mismatched measured seconds | Low: audit-only; it increases final-gate strictness but does not change benchmark execution | `scripts/audit_artifacts.ps1`, `scripts/test_official_manifest_suite_semantics_audit.ps1`, `scripts/test_audit_official_suite_validation_gates.ps1`, `scripts/verify_prebuild.ps1`, `docs/benchmark_methodology.md`, `docs/requirement_traceability.md`, `docs/completion_audit.md` | Prevents a completed official manifest from satisfying evidence requirements solely through correct file hashes when the benchmark JSON no longer forms the exact validated suite claimed by the manifest. |
| O-148 | Validate exact trusted matrix suites before completion | Kept for workflow; build pending | `audit_artifacts.ps1` now re-runs `validate_benchmark_suite.mjs` for every experiment declared by a completed trusted experiment matrix manifest after path/hash checks pass; `test_trusted_manifest_suite_semantics_audit.ps1` proves a matrix with matching paths/hashes is rejected when an experiment result has mismatched measured seconds, and the required-options fixture now uses valid metric JSON for its completed-package case | Low: audit-only; it raises trusted experiment evidence strictness without changing launch behavior | `scripts/audit_artifacts.ps1`, `scripts/test_trusted_manifest_suite_semantics_audit.ps1`, `scripts/test_trusted_manifest_required_options_audit.ps1`, `scripts/verify_prebuild.ps1`, `docs/benchmark_methodology.md`, `docs/requirement_traceability.md`, `docs/completion_audit.md` | Prevents fork-only trusted optimization evidence from being accepted solely because result files exist and hash correctly while their JSON no longer matches the claimed experiment suite. |
| O-149 | Require completed manifest suite-validation settings | Kept for workflow; build pending | `audit_artifacts.ps1` now rejects completed official and trusted manifests unless `suite_validation` explicitly records the strict validation gates the runners are expected to enforce, including checkout/build-args evidence, no-smoke performance evidence, hardware GPU metadata, software-renderer rejection, exact Chromium and fork revisions, expected browser executable fields, exact scene output files, and expected flag metadata; trusted matrices must also record the exact required scene list. `test_official_manifest_provenance_audit.ps1` and `test_trusted_manifest_provenance_audit.ps1` now mutate those settings to prove rejection, and the manifest artifact/package fixtures were updated to carry the full completed-manifest contract when testing later audit failures | Low: audit-only; it raises final evidence strictness but does not alter benchmark execution or runtime flags | `scripts/audit_artifacts.ps1`, `scripts/test_official_manifest_provenance_audit.ps1`, `scripts/test_trusted_manifest_provenance_audit.ps1`, `scripts/test_manifest_artifact_path_audit.ps1`, `scripts/test_manifest_package_audit.ps1`, `scripts/verify_prebuild.ps1`, `docs/benchmark_methodology.md`, `docs/requirement_traceability.md`, `docs/completion_audit.md` | Prevents a completed manifest from passing with valid-looking artifact paths and hashes while omitting the suite-validation promises that distinguish official same-revision hardware-GPU evidence from loose benchmark JSON. |
| O-150 | Verify completed manifest viewer patch metadata | Kept for workflow; build pending | `audit_artifacts.ps1` now includes `artifact_metadata.inputs.viewer_patch` in both official comparison and trusted experiment matrix path/hash checks, requiring it to match `chromium_patches/0001-draft-minimal-three-viewer-entrypoint.patch` on disk before completed manifests can pass; `test_manifest_artifact_path_audit.ps1` now requires fake viewer-patch metadata to be reported in the path/hash failure row, and exact-suite/package fixtures supply real viewer-patch metadata when testing later gates | Low: audit-only; it does not change the patch or benchmark execution but makes fork evidence provenance stricter | `scripts/audit_artifacts.ps1`, `scripts/test_manifest_artifact_path_audit.ps1`, `scripts/test_official_manifest_suite_semantics_audit.ps1`, `scripts/test_trusted_manifest_suite_semantics_audit.ps1`, `scripts/test_trusted_manifest_required_options_audit.ps1`, `docs/benchmark_methodology.md`, `docs/requirement_traceability.md`, `docs/completion_audit.md` | Ensures a completed official or trusted manifest cannot claim a patch-derived fork revision while omitting or mis-hashing the actual viewer patch file used to define that fork revision. |
| O-151 | Validate completed manifest report content | Kept for workflow; build pending | `audit_artifacts.ps1` now rejects completed official and trusted manifests whose report files exist and hash correctly but do not look like the generated benchmark reports: official comparisons must include the benchmark comparison heading, strict official validation note, comparison table header, renderer rows, every manifest scene, and expected variant labels; trusted summaries/comparisons must include their expected headings, table headers, renderer rows, every manifest scene, and experiment labels. `test_official_manifest_suite_semantics_audit.ps1` and `test_trusted_manifest_suite_semantics_audit.ps1` mutate hashed report files and scene-incomplete reports to prove rejection, and trusted required-options fixtures now generate valid synthetic report content for completed-package cases | Low: audit-only; it only tightens final evidence validation after path/hash checks pass | `scripts/audit_artifacts.ps1`, `scripts/test_official_manifest_suite_semantics_audit.ps1`, `scripts/test_trusted_manifest_suite_semantics_audit.ps1`, `scripts/test_trusted_manifest_required_options_audit.ps1`, `docs/benchmark_methodology.md`, `docs/requirement_traceability.md`, `docs/completion_audit.md` | Prevents a completed manifest from satisfying human-readable report requirements with arbitrary or scene-incomplete text files that merely match their recorded hashes. |
| O-152 | Validate standalone official report file rows | Kept for workflow; build pending | The direct `Human-readable official comparison reports` checklist row now validates canonical WebGL2 and WebGPU comparison markdown content before marking the row done, requiring comparison headings, strict official validation notes, table headers, renderer rows, every required scene, and stock/fork labels; `test_official_report_file_content_audit.ps1` exercises missing, placeholder, scene-incomplete, and valid synthetic reports through `audit_artifacts.ps1 -ReportStateOnly` with test-only report path overrides | Low: audit-only; it does not alter report generation or benchmark execution, and completed manifests still perform stricter path/hash and suite validation | `scripts/audit_artifacts.ps1`, `scripts/test_official_report_file_content_audit.ps1`, `scripts/verify_prebuild.ps1`, `docs/benchmark_methodology.md`, `docs/requirement_traceability.md`, `docs/completion_audit.md` | Prevents placeholder official report files from making the standalone human-readable report row appear complete before the official comparison manifest is generated. |
| O-153 | Guard test-only official report path overrides | Kept for workflow; build pending | `test_audit_manifest_override_guard.ps1` now verifies both synthetic manifest path overrides and synthetic official WebGL2/WebGPU report path overrides fail unless `THREE_BROWSER_ALLOW_TEST_MANIFEST_OVERRIDES=1` is set; `audit_artifacts.ps1` reports the combined guard in the manifest override regression row | Low: audit/test-only; it protects real audits from accidental environment-variable redirection while still allowing focused synthetic report tests | `scripts/audit_artifacts.ps1`, `scripts/test_audit_manifest_override_guard.ps1`, `scripts/verify_prebuild.ps1`, `docs/benchmark_methodology.md` | Prevents test-only report path overrides from making a real artifact audit read fake comparison reports outside `benchmarks/reports`. |
| O-154 | Record standalone official report hashes | Kept for workflow; build pending | The direct human-readable official report checklist row now records `size_bytes` and `sha256` for both canonical WebGL2 and WebGPU comparison markdown files after content validation passes; `test_official_report_file_content_audit.ps1` asserts valid synthetic report evidence includes hashes | Low: audit-only; completed official manifests still provide the authoritative report metadata for final benchmark evidence | `scripts/audit_artifacts.ps1`, `scripts/test_official_report_file_content_audit.ps1`, `docs/benchmark_methodology.md`, `docs/completion_audit.md` | Makes the standalone report row auditable as concrete file evidence instead of only saying report content looks valid. |
| O-155 | Make summary reports variant-aware | Kept for workflow; build pending | `summarize_results.mjs` now writes a `Variant` column from `benchmark_variant`, and trusted matrix report-content validation requires each experiment label in the summary as well as the comparison report; `test_report_metric_coverage.ps1`, `test_trusted_manifest_suite_semantics_audit.ps1`, and `test_trusted_manifest_required_options_audit.ps1` cover the generated header and label checks | Low: reporting/audit-only; raw JSON and comparison reports remain the source of benchmark deltas | `scripts/summarize_results.mjs`, `scripts/audit_artifacts.ps1`, `scripts/test_report_metric_coverage.ps1`, `scripts/test_trusted_manifest_suite_semantics_audit.ps1`, `scripts/test_trusted_manifest_required_options_audit.ps1`, `docs/benchmark_methodology.md`, `docs/requirement_traceability.md`, `docs/completion_audit.md` | Prevents trusted experiment matrix summaries from aggregating multiple risky flag groups into indistinguishable scene/renderer rows. |
| O-156 | Validate official trace summary content | Kept for workflow; build pending | Completed official manifests with trace capture now validate the generated trace-summary markdown after artifact path/hash checks pass, requiring the trace summary heading, manifest trace path, total-events line, classified-event table, top-duration-event table, and caveat text; `test_official_manifest_suite_semantics_audit.ps1` and `test_official_manifest_trace_audit.ps1` cover completed synthetic trace manifests and reject hashed non-summary files | Low: audit/report-only; raw traces and sidecars remain the source of trace truth | `scripts/audit_artifacts.ps1`, `scripts/test_official_manifest_suite_semantics_audit.ps1`, `scripts/test_official_manifest_trace_audit.ps1`, `docs/benchmark_methodology.md`, `docs/requirement_traceability.md`, `docs/completion_audit.md` | Prevents completed trace evidence from satisfying the report side of the trace requirement with arbitrary markdown that happens to match recorded hashes. |
| O-157 | Keep stability behavior documentation pending until real long-run evidence exists | Kept for workflow; build pending | The artifact audit now validates `docs/stability_behavior.md` for WebGL/WebGPU loss, GPU process crash/restart, exact stock/fork one-hour labels, RSS threshold, and zero renderer resource growth terms, then keeps the row pending until both validated stock/fork one-hour stability rows exist and the document no longer contains pending-evidence language; `test_stability_result_thresholds.ps1` checks the audit wiring and required doc terms | Low: audit/documentation-only; it makes completion stricter but does not change runtime behavior | `scripts/audit_artifacts.ps1`, `scripts/test_stability_result_thresholds.ps1`, `docs/stability_behavior.md`, `docs/benchmark_methodology.md`, `docs/requirement_traceability.md`, `docs/completion_audit.md` | Prevents a placeholder stability document from satisfying the prompt requirement to document GPU process crash/restart, context loss, device loss, RSS, and resource growth behavior after the required one-hour stock/fork run. |

## Prompt Optimization Class Decisions

This table is the final audit surface for the optimization classes named in the objective. A row may move to `kept`, `reverted`, `blocked`, or `not useful` only after the fork builds, smoke/runtime tests pass, same-revision benchmark or trace evidence exists, and the measured effect and risk are recorded. Until then, these rows intentionally remain pending.

| Optimization class | Status | Measured effect | Risk | Relevant evidence | Notes |
| --- | --- | --- | --- | --- | --- |
| remove Chrome browser UI layer | pending measurement | Pending same-revision stock/fork benchmark evidence | Pending runtime risk assessment after fork smoke | Pending fork binary and official comparison artifacts | Content-shell target and toolbar-hiding patch are drafted but not measured. |
| minimal content shell style entrypoint | pending measurement | Pending same-revision stock/fork benchmark evidence | Pending runtime risk assessment after fork smoke | Pending fork binary and official comparison artifacts | Draft patch modifies content shell; completion needs built fork evidence. |
| single local trusted origin | pending measurement | Pending navigation-lock runtime evidence and benchmark impact | Pending runtime risk assessment after HTTP/file navigation smoke | Pending fork runtime, HTTP navigation, and file navigation smoke artifacts | Same-origin and file-directory confinement are statically tested only. |
| disable extensions | pending measurement | Pending stock/fork benchmark subset evidence | Pending measurement of hidden dependency risk | Pending official comparison artifacts | Content shell avoids Chrome extensions, but final decision must be measured. |
| disable sync | pending measurement | Pending stock/fork benchmark subset evidence | Pending measurement of hidden dependency risk | Pending official comparison artifacts | Content shell avoids Chrome sync, but final decision must be measured. |
| disable autofill | pending measurement | Pending stock/fork benchmark subset evidence | Pending measurement of hidden dependency risk | Pending official comparison artifacts | Runner disables Autofill server communication; source-level decision pending. |
| disable translate | pending measurement | Pending stock/fork benchmark subset evidence | Pending measurement of hidden dependency risk | Pending official comparison artifacts | Runner disables Translate; source-level decision pending. |
| disable spellcheck | pending measurement | Pending stock/fork benchmark subset evidence | Pending measurement of hidden dependency risk | Pending official comparison artifacts | Web platform feature candidate; do not remove before runtime smoke. |
| disable safe browsing services not needed for local trusted content | pending measurement | Pending stock/fork benchmark subset evidence | Pending measurement of hidden dependency risk | Pending official comparison artifacts | Content shell avoids Chrome Safe Browsing UI/services, but remaining hooks need audit. |
| disable downloads UI | pending measurement | Pending stock/fork benchmark subset evidence | Pending measurement of asset/download classification risk | Pending navigation smoke and official comparison artifacts | Download delegate stubbing is a post-build candidate. |
| disable history/bookmarks UI | pending measurement | Pending stock/fork benchmark subset evidence | Pending measurement of hidden dependency risk | Pending official comparison artifacts | Content shell avoids these UI layers; final decision must be measured. |
| disable unnecessary profile services | pending measurement | Pending stock/fork benchmark subset evidence | Pending measurement of service dependency risk | Pending official comparison artifacts | Requires content-shell browser context audit after baseline. |
| disable unnecessary background networking | pending measurement | Pending stock/fork benchmark subset evidence | Pending measurement of service dependency risk | Pending official comparison artifacts | Runner flags exist; source-level decision pending. |
| reduce renderer process overhead where possible | pending measurement | Pending CPU frame/submission benchmark evidence | Pending process-model stability risk assessment | Pending official comparison and trace artifacts | Requires trace-guided renderer/GPU IPC analysis. |
| evaluate single-process/in-process GPU | pending measurement | Pending trusted matrix benchmark evidence | Pending GPU crash isolation and stability risk assessment | Pending trusted experiment matrix artifacts | Flags are trusted-only aliases; no runtime measurement yet. |
| evaluate ANGLE backend choices | pending measurement | Pending trusted matrix benchmark evidence | Pending backend availability and driver risk assessment | Pending trusted experiment matrix artifacts | Windows D3D11 experiment is in the post-ATL handoff. |
| evaluate platform GPU backends | pending measurement | Pending backend benchmark and trace evidence | Pending platform/GPU/driver risk assessment | Pending trusted experiment matrix and trace artifacts | Vulkan/D3D11/GL are Windows candidates; Metal is non-Windows future work. |
| evaluate passthrough command decoder | pending measurement | Pending trusted matrix benchmark evidence | Pending WebGL validation/security and driver stability risk assessment | Pending trusted experiment matrix artifacts | Relaxed WebGL gate maps to Chromium pass-through decoder only in trusted mode. |
| evaluate WebGL validation overhead | pending measurement | Pending trusted matrix benchmark evidence | Pending security/correctness risk assessment | Pending trusted experiment matrix artifacts | Must compare default WebGL2 with relaxed/pass-through experiment. |
| evaluate shader compilation strategy | pending measurement | Pending shader-heavy benchmark and trace evidence | Pending startup-vs-hitch tradeoff assessment | Pending official comparison, trace, and warmup artifacts | Resource warmup controls exist but need paired measurements. |
| evaluate WebGPU pipeline caching/warmup | pending measurement | Pending WebGPU benchmark and trace evidence | Pending startup-vs-hitch tradeoff assessment | Pending official comparison, trace, and warmup artifacts | WebGPU timestamp support exists; Dawn/cache strategy not measured. |
| evaluate compositor bypass or simplified presentation path | pending measurement | Pending trace and latency evidence | Pending presentation correctness and platform risk assessment | Pending stock/fork trace artifacts | Current direct-presentation flag is reserved/no-op. |
| evaluate direct GPU texture presentation/export | pending measurement | Pending trace and latency evidence | Pending platform-specific presentation risk assessment | Pending stock/fork trace artifacts | Reserved gate only until a concrete design is implemented. |
| evaluate removing unused Blink modules | pending measurement | Pending build size/runtime benchmark evidence | Pending hidden Blink dependency risk assessment | Pending source experiment and official comparison artifacts | Must preserve Three.js, Canvas, WebGL2, WebGPU, fetch, ImageBitmap, and input APIs. |
| evaluate disabling layout/style/DOM features not required by viewer | pending measurement | Pending runtime smoke and benchmark evidence | Pending high-risk DOM/layout compatibility assessment | Pending source experiment and official comparison artifacts | DOM/layout/style remain required for the viewer shell until proven otherwise. |
| evaluate disabling media, printing, PDF, WebRTC, accessibility, password manager, payments | pending measurement | Pending build size/runtime benchmark evidence | Pending feature-specific compatibility and platform risk assessment | Pending source experiment and official comparison artifacts | Treat each subsystem separately after the first stock/fork comparison. |
| evaluate V8 flags | pending measurement | Pending startup and steady-state JS benchmark evidence | Pending JS performance/regression risk assessment | Pending official comparison artifacts | V8 is required and only runtime/build flag experiments are allowed. |
| evaluate memory allocator and process model choices | pending measurement | Pending RSS/startup/frame-variance benchmark evidence | Pending allocator/process stability risk assessment | Pending official comparison and stability artifacts | Do not combine allocator and process-model changes before isolated measurements. |

## Audit Workflow Notes

| ID | Decision | Status | Evidence | Notes |
| --- | --- | --- | --- | --- |
| O-158 | Keep patch notes pending until final official/trusted evidence exists | Kept for workflow; build pending | `scripts/test_patch_notes_completion_audit.ps1`; `scripts/audit_artifacts.ps1 -PatchStateOnly` patch-notes row | Draft patch notes are useful during prebuild work, but they must not count as final patch-series evidence until post-ATL official comparison and trusted experiment matrix manifests exist and draft-only language is removed. |
| O-159 | Keep removed-subsystem register pending until final official/trusted evidence exists | Kept for workflow; build pending | `scripts/test_removed_subsystems_completion_audit.ps1`; `scripts/audit_artifacts.ps1 -OptimizationOnly` removed-subsystem final-register row | The structural removed-subsystem register documents rationale and risk, but it must not count as final removed/disabled subsystem evidence until official comparison and trusted experiment matrix manifests exist and pending-only register language is replaced with measured decisions. |
| O-160 | Keep final documentation pending until measured official/trusted/stability evidence exists | Kept for workflow; build pending | `scripts/test_documentation_completion_audit.ps1`; `scripts/audit_artifacts.ps1 -DocumentationOnly` final documentation row | Documentation file existence is useful prebuild evidence, but final docs must stay incomplete until stock/fork official comparison, trusted experiment matrix, and one-hour stability artifacts exist and blocker/pending language is replaced with measured outcomes. |
| O-161 | Validate local-only viewer bundle integrity | Kept for harness; runtime pending | `scripts/test_viewer_bundle_integrity.ps1`; `viewer/dist/index.html`; `viewer/dist/assets/checker.svg`; `viewer/dist/assets/models/cube-stress.gltf` | The viewer bundle is a local Vite output with relative asset references and pinned Three.js inputs, reducing the chance of hidden CDN/network dependencies before stock/fork runtime smoke can validate actual browser loading. |
| O-162 | Guard against premature performance-win claims | Kept for workflow; build pending | `scripts/test_performance_claim_guard.ps1`; `scripts/audit_artifacts.ps1 -DocumentationOnly` premature-performance-claims row | Authored docs may describe objectives, hypotheses, blockers, and pending evidence, but they must not state stock/fork performance wins until the official same-revision comparison manifest is completed. |
| O-163 | Reject stale source-investigation verification revisions | Kept for workflow; build pending | `scripts/test_source_investigation_revision_guard.ps1`; `scripts/test_source_investigation_paths.ps1`; `docs/source_investigation.md` | A Chromium pin refresh now has a negative regression that proves a copied source-investigation doc with an old verification SHA fails, instead of only checking that source paths still exist. |
| O-164 | Regression-test ATL blocker audit evidence wording | Kept for workflow; build pending | `scripts/test_atl_blocker_audit.ps1`; `scripts/audit_artifacts.ps1 -BuildStateOnly`; `benchmarks/reports/prebuild-environment.json` | The active ATL/MFC blocker row now has synthetic coverage for current Siso failure logs, stale logs after a later GN generation, and prerequisite-only evidence, so the checklist cannot overstate stale build failures. |
| O-165 | Verify ATL remediation before build handoff | Kept for workflow; build pending | `scripts/install_vs_atl.ps1`; `scripts/test_atl_remediation_handoff.ps1`; `README.md`; `docs/build.md`; `docs/requirement_traceability.md`; `docs/completion_audit.md` | The elevated installer now checks for `atldef.h` and `vswhere -requires Microsoft.VisualStudio.Component.VC.ATLMFC` after the Visual Studio Installer returns, so it cannot silently hand off to the post-ATL build pipeline if the required component is still missing. |
| O-166 | Keep upstream freshness pending when the probe is unavailable | Kept for workflow; build pending | `scripts/audit_artifacts.ps1`; `scripts/test_upstream_freshness_audit.ps1`; `benchmarks/reports/prebuild-environment.json` | A current upstream HEAD probe failure no longer counts as freshness evidence before completed official stock/fork evidence exists; the audit now accepts that case only after the refreshed pin has completed official same-revision results. |
| O-167 | Require existing artifacts for post-ATL resume skips | Kept for workflow; build pending | `scripts/run_post_atl_pipeline.ps1`; `scripts/test_post_atl_pipeline_dry_run.ps1`; `docs/build.md`; `docs/benchmark_methodology.md` | Non-dry resume modes now fail before prebuild verification if `-SkipBaselineBuild`, `-SkipForkBuild`, or `-SkipPackage` would rely on missing binaries, GN args, or staged packages, preventing a resumed pipeline from silently dropping required stock/fork/package evidence. |
| O-168 | Preflight official comparison package directories | Kept for workflow; build pending | `scripts/run_official_comparison.ps1`; `scripts/test_dry_run_manifests.ps1`; `docs/build.md`; `docs/benchmark_methodology.md` | A real official comparison run that supplies `-BaselinePackageDir` or `-ForkPackageDir` now verifies the staged package shape before smoke or benchmark execution, so package-size evidence cannot silently fall back to missing or incomplete directories. |
| O-169 | Preflight trusted experiment package directory | Kept for workflow; build pending | `scripts/run_trusted_experiment_matrix.ps1`; `scripts/test_dry_run_manifests.ps1`; `docs/build.md`; `docs/benchmark_methodology.md` | A real trusted experiment matrix run that supplies `-PackageDir` now verifies the staged fork package shape before per-flag benchmark execution, matching the official comparison preflight and protecting package-size evidence for risky trusted modes. |
| O-170 | Preflight long-stability package directory | Kept for workflow; build pending | `scripts/run_long_stability.ps1`; `scripts/test_stability_result_thresholds.ps1`; `docs/build.md`; `docs/benchmark_methodology.md` | A one-hour stability run that supplies `-PackageDir` now verifies the staged package shape before launching the browser, so final stability package-size evidence cannot depend on a missing or incomplete package directory. |
| O-171 | Preflight official/trusted/stability input files | Kept for workflow; build pending | `scripts/run_official_comparison.ps1`; `scripts/run_trusted_experiment_matrix.ps1`; `scripts/run_long_stability.ps1`; `scripts/test_dry_run_manifests.ps1`; `scripts/test_stability_result_thresholds.ps1`; `docs/build.md`; `docs/benchmark_methodology.md` | Real official comparison, trusted experiment, and long-stability wrappers now reject missing or empty browser/GN-args input files before smoke, benchmark, trace, summary, or validation work starts, so final evidence cannot be generated from placeholder executables or stale empty args files. |
| O-172 | Bind staged packages to benchmarked executables | Kept for workflow; build pending | `scripts/run_official_comparison.ps1`; `scripts/run_trusted_experiment_matrix.ps1`; `scripts/run_long_stability.ps1`; `scripts/test_dry_run_manifests.ps1`; `scripts/test_stability_result_thresholds.ps1`; `docs/build.md`; `docs/benchmark_methodology.md` | Real official, trusted, and long-stability runs now compare the packaged `content_shell.exe` SHA-256 against the browser executable under test, preventing package-size evidence from being supplied by a stale or unrelated staged package. |
| O-173 | Guard post-ATL package-skip resume evidence | Kept for workflow; build pending | `scripts/run_post_atl_pipeline.ps1`; `scripts/test_post_atl_pipeline_dry_run.ps1`; `docs/build.md`; `docs/benchmark_methodology.md` | `-SkipPackage` now requires the matching stock/fork build skips and verifies reused package executable hashes before prebuild verification when later official, trusted, or stability steps would consume those packages, so a resumed completion run cannot rebuild binaries while reusing stale package evidence. |
| O-174 | Audit manifest package executable hashes | Kept for workflow; build pending | `scripts/audit_artifacts.ps1`; `scripts/test_manifest_artifact_path_audit.ps1`; `scripts/test_trusted_manifest_required_options_audit.ps1`; `scripts/test_trusted_manifest_suite_semantics_audit.ps1`; `docs/benchmark_methodology.md` | Completed official and trusted manifests now fail the artifact audit if their staged package `content_shell.exe` hash no longer matches the manifest browser hash, so stale package evidence cannot become accepted after package directories are modified on disk. |
| O-175 | Bind long-stability raw results to expected browser executables | Kept for workflow; build pending | `scripts/validate_stability_result.mjs`; `scripts/run_long_stability.ps1`; `scripts/audit_artifacts.ps1 -StabilityOnly`; `scripts/test_stability_result_thresholds.ps1`; `docs/benchmark_methodology.md`; `docs/requirement_traceability.md`; `docs/completion_audit.md` | One-hour stock/fork stability rows now require `browser_executable` in the raw benchmark JSON to match the expected stock or fork `content_shell.exe`, and the regression test proves the stability-only artifact audit rejects a canonical wrong-browser long-run JSON instead of accepting matching revision, launch flags, and package-size fields. |
| O-176 | Bind benchmark scene-suite results to expected browser executables | Kept for workflow; build pending | `scripts/validate_benchmark_suite.mjs`; `scripts/run_official_comparison.ps1`; `scripts/run_trusted_experiment_matrix.ps1`; `scripts/audit_artifacts.ps1`; `scripts/test_benchmark_suite_flag_metadata.ps1`; `scripts/test_audit_official_suite_validation_gates.ps1`; `scripts/test_official_manifest_suite_semantics_audit.ps1`; `scripts/test_trusted_manifest_suite_semantics_audit.ps1`; `docs/benchmark_methodology.md`; `docs/requirement_traceability.md`; `docs/completion_audit.md` | Official and trusted scene-suite validation now rejects benchmark JSON whose `browser_executable` does not match the stock or fork browser under test, and manifest semantic audit tests prove matching result hashes are insufficient when exact result files came from the wrong executable. |
| O-177 | Record expected-browser suite-validation fields in completed manifests | Kept for workflow; build pending | `scripts/run_official_comparison.ps1`; `scripts/run_trusted_experiment_matrix.ps1`; `scripts/audit_artifacts.ps1`; `scripts/test_official_manifest_provenance_audit.ps1`; `scripts/test_trusted_manifest_provenance_audit.ps1`; `scripts/test_manifest_artifact_path_audit.ps1`; `docs/benchmark_methodology.md`; `docs/requirement_traceability.md`; `docs/completion_audit.md` | Real official manifests now persist `expected_baseline_browser` and `expected_fork_browser` in `suite_validation`, trusted matrix manifests persist `expected_browser`, and the completed-manifest audit compares those fields to the manifest browser paths before exact-suite semantic validation can pass. |
| O-178 | Add runtime smoke coverage for basic input events | Kept for harness; stock/fork runtime pending | `scripts/run_smoke_tests.mjs`; `scripts/validate_smoke_result.mjs`; `scripts/test_smoke_coverage.ps1`; `scripts/test_smoke_detail_validation.ps1`; `benchmarks/raw/smoke-installed-chrome-v11-stability-smoke.json`; `benchmarks/reports/smoke-installed-chrome-v11-stability-smoke.md`; `docs/benchmark_methodology.md`; `docs/requirement_traceability.md`; `docs/completion_audit.md` | Runtime smoke now requires a `basic_input_events` pass covering pointer down/move/up, wheel, and keydown delivery, so final stock/fork runtime smoke cannot satisfy the input-events requirement through source inspection alone. |
| O-179 | Add runtime smoke coverage for core web platform APIs | Kept for harness; stock/fork runtime pending | `scripts/run_smoke_tests.mjs`; `scripts/validate_smoke_result.mjs`; `scripts/test_smoke_coverage.ps1`; `scripts/test_smoke_detail_validation.ps1`; `benchmarks/raw/smoke-installed-chrome-v11-stability-smoke.json`; `benchmarks/reports/smoke-installed-chrome-v11-stability-smoke.md`; `docs/benchmark_methodology.md`; `docs/requirement_traceability.md`; `docs/completion_audit.md` | Runtime smoke now requires `web_platform_basics`, proving `requestAnimationFrame`, Canvas 2D, local `fetch`, and `performance.now` before a stock or fork runtime smoke artifact can satisfy the runtime-completion API surface. |
| O-180 | Semantically validate runtime web-platform and input smoke details | Kept for workflow; stock/fork runtime pending | `scripts/validate_smoke_result.mjs`; `scripts/test_smoke_coverage.ps1`; `scripts/test_smoke_detail_validation.ps1`; `scripts/test_official_manifest_runtime_audit.ps1`; `docs/benchmark_methodology.md`; `docs/requirement_traceability.md`; `docs/completion_audit.md` | Runtime smoke validation now rejects pass-labelled `web_platform_basics` or `basic_input_events` results with empty or malformed detail fields, so completed manifests cannot satisfy the API/input runtime surface with labels alone. |
| O-181 | Add Three.js WebGPU renderer runtime smoke | Kept for harness; stock/fork runtime pending | `scripts/run_smoke_tests.mjs`; `scripts/validate_smoke_result.mjs`; `scripts/run_official_comparison.ps1`; `scripts/test_smoke_coverage.ps1`; `scripts/test_smoke_detail_validation.ps1`; `scripts/test_dry_run_manifests.ps1`; `docs/benchmark_methodology.md`; `docs/requirement_traceability.md`; `docs/completion_audit.md` | Runtime smoke now includes an optional Three.js `WebGPURenderer` draw with semantic detail validation, and official WebGPU comparisons pass `--require-webgpu` so WebGPU adapter/device, device-loss, and Three.js WebGPU renderer smoke cannot be skipped when WebGPU suites are requested. |
| O-182 | Replay required WebGPU runtime smoke in completed-manifest audits | Kept for workflow; stock/fork runtime pending | `scripts/validate_smoke_result.mjs`; `scripts/run_official_comparison.ps1`; `scripts/audit_artifacts.ps1`; `scripts/test_smoke_coverage.ps1`; `scripts/test_smoke_detail_validation.ps1`; `scripts/test_dry_run_manifests.ps1`; `scripts/test_official_manifest_runtime_audit.ps1`; `scripts/test_official_manifest_provenance_audit.ps1`; `docs/benchmark_methodology.md`; `docs/requirement_traceability.md` | `validate_smoke_result.mjs --require-webgpu` now rejects skipped WebGPU adapter/device, device-loss, or Three.js WebGPU renderer smoke, official manifests record `require_webgpu_runtime_smoke=true` for WebGPU runs, and artifact audit replays smoke validation with that requirement for completed WebGPU manifests. |
| O-183 | Require WebGPU in installed-Chrome runtime smoke harness evidence | Kept for harness; stock/fork runtime pending | `scripts/verify_prebuild.ps1`; `scripts/audit_artifacts.ps1`; `scripts/test_smoke_coverage.ps1`; `benchmarks/raw/smoke-installed-chrome-v11-stability-smoke.json`; `docs/benchmark_methodology.md`; `docs/requirement_traceability.md` | The direct installed-Chrome smoke verifier and artifact row now validate the v11 smoke artifact with `--require-webgpu`, so harness evidence cannot count if WebGPU adapter/device, device-loss, or Three.js WebGPU renderer smoke becomes skipped. |
| O-184 | Require WebGPU in direct stock/fork runtime smoke rows | Kept for workflow; stock/fork runtime pending | `scripts/audit_artifacts.ps1`; `scripts/test_smoke_coverage.ps1`; `docs/benchmark_methodology.md`; `docs/requirement_traceability.md`; `docs/completion_audit.md` | Direct stock and fork runtime smoke checklist rows now require expected browser, launch-mode metadata, and `--require-webgpu`, matching the completion-oriented official WebGPU workflow instead of allowing generic runtime smoke to close those rows. |
| O-185 | Semantically validate WebGPU adapter/device and device-loss smoke details | Kept for workflow; stock/fork runtime pending | `scripts/validate_smoke_result.mjs`; `scripts/test_smoke_coverage.ps1`; `scripts/test_smoke_detail_validation.ps1`; `benchmarks/raw/smoke-installed-chrome-v11-stability-smoke.json`; `docs/benchmark_methodology.md`; `docs/requirement_traceability.md`; `docs/completion_audit.md` | Runtime smoke validation now rejects pass-labelled WebGPU adapter/device smoke with malformed feature or limit details and rejects WebGPU device-loss smoke unless explicit `GPUDevice.destroy()` reports `reason=destroyed`, so WebGPU runtime evidence is semantic rather than label-only. |
| O-186 | Guard Chromium-only fork scope in authored docs | Kept for workflow; build pending | `scripts/test_chromium_scope_guard.ps1`; `scripts/verify_prebuild.ps1`; `scripts/audit_artifacts.ps1`; `docs/benchmark_methodology.md`; `docs/requirement_traceability.md`; `docs/completion_audit.md` | Authored README/docs/patch notes are now scanned for non-Chromium alternative recommendations so the fork-first scope cannot drift toward CEF, Electron, native engines, Unity, Unreal, raw Vulkan, or raw WebGPU except as benchmark references or explicit non-recommendations. |
| O-187 | Verify viewer entrypoint launch precedence in the draft patch | Kept for workflow; fork runtime pending | `scripts/test_viewer_patch_entrypoint.ps1`; `scripts/verify_prebuild.ps1`; `scripts/audit_artifacts.ps1`; `chromium_patches/0001-draft-minimal-three-viewer-entrypoint.patch`; `docs/benchmark_methodology.md`; `docs/requirement_traceability.md`; `docs/completion_audit.md` | Static patch validation now proves `--viewer-app-url` is handled before positional URLs or content shell's default startup URL, keeps toolbar suppression tied to viewer mode, converts local paths to `file://` launches, and blocks new tab/window creation in viewer navigation-lock mode. |
| O-188 | Fail closed against software rasterizer fallback in primary launchers | Kept for workflow; stock/fork runtime pending | `scripts/run_benchmark.mjs`; `scripts/run_smoke_tests.mjs`; `scripts/run_trace_capture.mjs`; `scripts/stage_viewer_package.ps1`; `scripts/test_hardware_gpu_launch_flags.ps1`; `scripts/test_stage_viewer_package.ps1`; `docs/benchmark_methodology.md`; `docs/trusted_content_flags.md`; `docs/requirement_traceability.md`; `docs/completion_audit.md` | Benchmark, smoke, trace, and packaged viewer launchers now include `--disable-software-rasterizer`, and a regression test verifies both the launch flag and post-run software-renderer rejection terms so primary evidence is less likely to silently fall back to Chromium software rendering. |
| O-189 | Record effective browser launch flags in benchmark, smoke, and trace artifacts | Kept for workflow; stock/fork runtime pending | `scripts/run_benchmark.mjs`; `scripts/run_smoke_tests.mjs`; `scripts/run_trace_capture.mjs`; `scripts/validate_metrics.mjs`; `scripts/validate_smoke_result.mjs`; `scripts/validate_trace_result.mjs`; `scripts/test_benchmark_flag_metadata.ps1`; `scripts/test_hardware_gpu_launch_flags.ps1`; `scripts/test_smoke_coverage.ps1`; `docs/benchmark_methodology.md`; `docs/trusted_content_flags.md` | Raw benchmark JSON, runtime smoke JSON, and trace sidecars now record `browser_flags` as the full effective launch argument list and preserve user-supplied pass-through additions as `browser_extra_flags`, making future stock/fork evidence auditable for hardware-GPU and trusted-viewer launch policy. |
| O-190 | Require hardware-GPU launch flag metadata in final evidence validators | Kept for workflow; stock/fork runtime pending | `scripts/validate_benchmark_suite.mjs`; `scripts/validate_smoke_result.mjs`; `scripts/validate_trace_result.mjs`; `scripts/validate_stability_result.mjs`; `scripts/run_official_comparison.ps1`; `scripts/run_trusted_experiment_matrix.ps1`; `scripts/run_long_stability.ps1`; `scripts/audit_artifacts.ps1`; `scripts/test_benchmark_suite_flag_metadata.ps1`; `scripts/test_trace_result_validation.ps1`; `scripts/test_smoke_detail_validation.ps1`; `scripts/test_stability_result_thresholds.ps1`; `scripts/test_hardware_gpu_launch_flags.ps1`; `docs/benchmark_methodology.md`; `docs/trusted_content_flags.md` | Official/trusted suites, runtime smoke, trace sidecars, and one-hour stability validation can now require `--disable-software-rasterizer` in recorded `browser_flags`, preventing final stock/fork evidence from relying only on static launcher inspection for the hardware-GPU fail-closed policy. |
| O-191 | Restamp source-investigation evidence during Chromium pin refresh | Kept for workflow; build pending | `scripts/refresh_chromium_pin.ps1`; `scripts/test_refresh_chromium_pin_dry_run.ps1`; `scripts/test_source_investigation_paths.ps1`; `docs/source_investigation.md`; `docs/build.md`; `docs/rebase_strategy.md`; `docs/requirement_traceability.md`; `docs/completion_audit.md` | A real pin refresh now updates the source-investigation verification marker and immediately validates documented Chromium paths and sentinel symbols; if validation fails, the helper restores the previous document text. The dry-run regression also verifies the source map remains unmodified under `-DryRun`, so `run_post_atl_pipeline.ps1 -RefreshChromiumPin` does not fail later verification solely because the investigation map was stamped to the previous pin. |
| O-192 | Complete comparison-report delta coverage for collected memory and dropped-frame metrics | Kept for workflow; build pending | `scripts/compare_results.mjs`; `scripts/test_report_metric_coverage.ps1`; `docs/benchmark_methodology.md` | Official comparison markdown now includes delta columns for `dropped_frames`, `js_heap_mb`, and `gpu_memory_mb`, matching the required JSON schema and summary report coverage. This keeps the human-readable stock/fork report from hiding memory or dropped-frame regressions even when the raw JSON remains the source of truth. |
