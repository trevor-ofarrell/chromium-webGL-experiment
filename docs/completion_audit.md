# Completion Audit

Date: 2026-05-18

Objective: create a Chromium-derived executable that launches into a bundled/local Three.js viewer, preserves Blink/V8/GPU/WebGL2/WebGPU support, strips unrelated browser product behavior where justified, and proves performance with same-revision stock-vs-fork benchmarks.

Status: not complete.

The detailed prompt-to-artifact checklist is generated by:

```powershell
.\scripts\audit_artifacts.ps1
```

Current generated checklist: `docs/prompt_to_artifact_checklist.md`.

## Primary Deliverables

| Deliverable | Status | Evidence | Missing |
| --- | --- | --- | --- |
| Runnable Chromium-derived 3D viewer binary | Blocked | Draft patch applies cleanly; fork build wrapper exists | `content_shell` baseline and fork cannot build until ATL/MFC headers are installed |
| Patch series or branch with clear changes | Partial | `chromium_patches/0001-draft-minimal-three-viewer-entrypoint.patch`; patch applies cleanly and now normalizes raw absolute viewer paths before generic URL parsing | Needs built/tested patch commits after baseline capture |
| Local Three.js benchmark viewer app | Done | `viewer/`, `viewer/dist/index.html`, `npm run build` passes; `scripts/test_viewer_bundle_integrity.ps1` verifies local-only dist references, pinned Three.js/Vite inputs, bundled checker/glTF assets, and no remote source imports/fetches; `gltf-loader-stress` covers a bundled `GLTFLoader` path | Add larger imported real-world scenes later |
| Benchmark report comparing stock Chromium and fork | Pending | Installed-Chrome smoke reports only | Same-revision stock and fork binaries/results |
| Optimization table | Partial | `docs/optimization_log.md` now includes `Prompt Optimization Class Decisions`; `scripts/test_optimization_decision_audit.ps1` verifies the final audit keeps pending rows incomplete and accepts only structured final decisions | Needs measured keep/revert/block/not-useful decisions from stock/fork experiments |
| Remaining bottleneck statement | Pending | Known build blocker documented | Needs post-benchmark analysis |
| Strict reproduction guide | Partial | `docs/build.md`, scripts | Needs final stock/fork commands and artifacts after successful build |

## Build Completion

| Requirement | Status | Evidence |
| --- | --- | --- |
| Start from current Chromium source | Partial | Active pin is stored in `.chromium_revision`; `scripts\refresh_chromium_pin.ps1` refreshes it to the currently observed Chromium `origin HEAD`, syncs hooks, regenerates GN metadata, rewrites the environment manifest, checks patch applicability, and restamps the source-investigation map after source-path validation |
| Release build succeeds | Blocked | `gn gen` succeeds for baseline, default-fork, and trusted/aggressive output profiles; baseline `content_shell` build remains blocked by missing ATL/MFC headers (`base/win/atl_throw.h` includes `atldef.h`); the blocker is recorded in `benchmarks/reports/prebuild-environment.json` |
| Reproducible GN args | Done | `build/gn_args/baseline_content_shell.gn`, `fork_safe_content_shell.gn`, `fork_trusted_aggressive.gn` |
| Build instructions from clean checkout | Partial | `docs/build.md`, `scripts/bootstrap_chromium.ps1`, `scripts/build_chromium.ps1`, `scripts/run_post_atl_pipeline.ps1`; `benchmarks/reports/prebuild-environment.json` captures current host/toolchain state; `scripts/test_post_atl_pipeline_dry_run.ps1` verifies the one-command post-ATL dry-run includes Chromium pin refresh, official comparison, trusted matrix, stock/fork long-stability, non-mutating patched-source baseline guard, existing-artifact checks for resume skips, and final audit handoffs; `scripts/test_audit_fail_on_incomplete.ps1` verifies the final audit gate fails while required artifacts are missing | Need validated post-ATL clean build |

## Runtime Completion

| Requirement | Status | Evidence |
| --- | --- | --- |
| Launch directly into bundled viewer | Partial | Draft `--viewer-app-url` patch and `scripts/build_viewer_fork.ps1`; `scripts/test_viewer_patch_entrypoint.ps1` verifies the viewer branch precedes positional command-line URLs and content shell's default startup URL | Needs built fork binary |
| No browser chrome | Partial | Patch hides content-shell toolbar when `--viewer-app-url` is present; `scripts/test_viewer_patch_entrypoint.ps1` verifies the toolbar suppression remains tied to viewer mode | Needs runtime verification |
| Local/bundled scenes without network dependency | Partial | Viewer builds locally; benchmark runner serves loopback local bundle; `--viewerFileMode` smoke validates file-mode glTF loading when trusted file access is enabled | Fork still needs runtime validation of the trusted file-launch alias |
| WebGL2 Three.js renderer | Done for harness | `smoke-installed-chrome-v6-*webgl2.json`; runtime smoke WebGL2 pass | Needs same-revision Chromium/fork run |
| WebGPU Three.js renderer | Partial | `smoke-installed-chrome-v7-webgpu-*.json`; runtime smoke WebGPU adapter/device pass with feature/limit detail validation; runtime smoke validates WebGPU device-loss details and includes a Three.js `WebGPURenderer` render check when WebGPU is available; `docs/webgpu_scene_coverage.md` marks non-equivalent scenes | Replace WebGPU fallbacks and run same-revision binaries |
| `requestAnimationFrame`, Canvas, `fetch`, texture loading, `createImageBitmap`, `performance.now`, input | Partial | Viewer code uses these; `run_smoke_tests.mjs` covers launch/WebGL2/web-platform basics/basic input/WebGPU/Three.js WebGPU render/texture/ImageBitmap/shader/benchmark and records launch-mode plus browser executable metadata; `validate_smoke_result.mjs` verifies required smoke tests passed, checks web-platform/input/WebGPU adapter/device/loss/render, benchmark, and navigation/file-navigation detail sanity, requires browser-mode metadata and the baseline browser path for stock runtime smoke, requires viewer-mode/trusted-content metadata plus the fork browser path for fork runtime smoke evidence, and direct stock/fork runtime rows require WebGPU smoke | Run against stock/fork binaries |
| Block arbitrary navigation | Partial | Draft navigation throttle and new-window block; `scripts/test_viewer_patch_navigation_lock.ps1` statically verifies startup URL/path handling, toolbar hiding, navigation throttle registration, same-origin checks, file-directory confinement, and non-current-tab blocking; HTTP-origin and file-mode navigation lock scripts cover startup URL/path, same-origin navigation, file directory confinement, loopback cross-origin blocking, deterministic external HTTP blocking through a non-loopback IPv4 interface, and `window.open` blocking; patch handles raw absolute local viewer paths as file launches; `benchmarks/reports/prebuild-environment.json` records whether the host has the required non-loopback IPv4 interface | Needs fork binary runtime result |

## Performance Completion

| Requirement | Status | Evidence |
| --- | --- | --- |
| Compare stock Chromium, fork default, fork trusted aggressive | Pending | No stock/fork binaries yet |
| JSON metric schema | Done | `scripts/validate_metrics.mjs`; current-schema smoke JSON validates required fields, renderer/scene names, numeric sanity, optional timing/stability fields, and frame-time arrays |
| Human-readable report | Done for smoke, pending official | `benchmarks/reports/smoke-installed-chrome-v6-benchmark-summary.md`; summary reports include a `Variant` column for aggregated experiment evidence; `scripts/test_official_report_file_content_audit.ps1` verifies the official WebGL2/WebGPU report checklist row rejects missing, placeholder, or scene-incomplete comparison reports and records hashes for valid reports |
| Official comparison workflow | Pending binaries | `scripts/run_official_comparison.ps1 -DryRun` emits the expected smoke, benchmark, complete-suite validation, summary, strict official comparison, and manifest commands, including aggressive WebGPU when WebGPU and aggressive mode are both requested; WebGPU official runs require WebGPU runtime smoke during immediate validation and completed-manifest replay; primary launchers now include `--disable-software-rasterizer` and benchmark/smoke/trace artifacts record effective `browser_flags` plus pass-through `browser_extra_flags`, with final benchmark, smoke, trace, and stability validators requiring that launch flag for official evidence and `scripts/test_hardware_gpu_launch_flags.ps1` checking the fail-closed launch policy, launch-argument metadata, and software-renderer validator terms; `scripts/test_dry_run_manifests.ps1` validates the planned official manifest shape; `scripts/test_revision_validation.ps1` verifies suite/stability rejection of stale Chromium revisions; `scripts/test_benchmark_suite_duration_validation.ps1` verifies suite-level rejection of mismatched measured/warmup seconds; `scripts/test_benchmark_suite_rejects_software_rendering.ps1` verifies suite-level rejection of missing GPU metadata and known software-rendered inputs; `scripts/test_benchmark_suite_flag_metadata.ps1` verifies suite-level rejection of wrong browser executables, viewer flag metadata, and missing required launch flags; `scripts/test_strict_official_rejects_software_rendering.ps1` verifies official reports reject missing GPU metadata, missing/mismatched fork revision, known SwiftShader/WARP/llvmpipe/software-rendered inputs, and mismatched measured/warmup seconds; `scripts/test_official_report_file_content_audit.ps1` verifies standalone official report rows reject missing, placeholder, or scene-incomplete comparison reports; `scripts/test_official_manifest_required_options_audit.ps1` verifies final official evidence must include WebGPU, aggressive GPU, trace capture, and baseline/fork package dirs; `scripts/test_official_manifest_provenance_audit.ps1` verifies completed manifests reject stale pin-refresh provenance and missing suite-validation gate settings including required WebGPU runtime-smoke settings for WebGPU manifests; `scripts/test_official_manifest_suite_semantics_audit.ps1` verifies completed manifests reject exact benchmark result files that fail suite validation, including wrong browser executable metadata, missing required launch flags, hashed report content that is not a comparison report, scene-incomplete reports, and invalid trace-summary markdown; `scripts/test_manifest_artifact_path_audit.ps1` verifies completed manifests reject fake artifact paths/hashes including viewer-patch metadata; `scripts/test_official_manifest_webgpu_audit.ps1` verifies completed WebGPU manifests require WebGPU result hashes; `scripts/test_official_manifest_aggressive_audit.ps1` verifies completed aggressive manifests require WebGL2/WebGPU result hashes and backend metadata; `scripts/test_official_manifest_runtime_audit.ps1` verifies completed manifests reject skipped, missing, or semantically invalid runtime/navigation smoke evidence; real manifests require exact result/report/smoke paths plus browser/build-args/viewer-patch/result/report/smoke hashes, valid comparison report content with every manifest scene, valid trace summary content when capture is enabled, explicit suite-validation gate settings including `expected_baseline_browser` and `expected_fork_browser`, and semantic suite validation for exact result files against the expected stock/fork browser executables | Run after baseline/fork binaries exist |
| Trusted experiment matrix workflow | Pending fork binary | `scripts/run_trusted_experiment_matrix.ps1 -DryRun` emits per-experiment benchmark commands, suite-validation commands, exact summary/compare inputs, and a planned manifest; `scripts/test_dry_run_manifests.ps1` validates the planned trusted manifest shape; suite validation requires the expected fork browser executable, exact duration/warmup, GPU metadata, and software-renderer rejection; `audit_artifacts.ps1` requires a completed non-dry manifest with current fork revision, pin-refresh provenance, browser/build-args/viewer-patch hashes, hashed result JSON, summary/comparison reports with valid report content, every manifest scene, and experiment labels, staged package metadata, explicit suite-validation gate settings including `expected_browser`, and semantic suite validation for each exact experiment result-file list; `scripts/test_trusted_manifest_provenance_audit.ps1` verifies expected-scene, expected-browser, and expected-flag-metadata suite setting rejection; `scripts/test_trusted_manifest_suite_semantics_audit.ps1` verifies matching trusted result hashes, report hashes, scene-incomplete reports, wrong browser executable metadata, or summary reports missing an experiment label alone are not enough | Run after fork binary exists |
| Patch-state audit regression | Done | `scripts/test_patch_state_audit.ps1` verifies that manifest/audit logic accepts an already-applied fork patch using temporary files and leaves `src` untouched | Re-run if patch/audit logic changes |
| Patch notes completion audit | Pending final evidence | `scripts/test_patch_notes_completion_audit.ps1` verifies the patch-notes artifact row remains pending while official comparison or trusted experiment matrix manifests are missing, or while patch notes still contain draft-only language | Replace draft patch notes with final measured patch-series notes after post-ATL stock/fork and trusted runs complete |
| Required scene families | Done for harness, pending official | `many-draw-calls`, `instancing`, `shader-heavy`, `texture-streaming`, `postprocessing`, `large-static`, `gltf-loader-stress`; current WebGPU v17 smoke covers all seven scenes and passes suite validation | Same-revision stock/fork suites are still pending; WebGPU shader/effect parity notes remain in `docs/webgpu_scene_coverage.md` |
| GPU frame time where available | Partial | WebGL timer query path records values on v6 smoke; WebGPU timestamp-query instrumentation records `avg_gpu_frame_ms` in `smoke-installed-chrome-v14-webgpu-timestamp-instancing-webgpu.json` | Same-revision stock/fork WebGPU timing evidence still required |
| Startup, memory, size metrics | Partial | Runner records startup, process RSS start/end/peak/delta, executable size, viewer bundle size, optional package size | Official stock/fork package staging pending |
| Package staging | Done for synthetic regression, pending binaries | `scripts/test_stage_viewer_package.ps1` validates staged runtime asset copies, selected runtime directories, viewer bundle copy, `-Clean`, launcher switches, and package size output using synthetic inputs; `scripts/test_manifest_package_audit.ps1` verifies completed manifests reject missing package metadata when package dirs are supplied; `scripts/test_benchmark_suite_package_validation.ps1` verifies packaged suite validation rejects missing or non-positive `package_size_mb`; `scripts/run_post_atl_pipeline.ps1` stages baseline and fork package directories and passes them to official comparison and long-stability runs, while trusted experiments use the fork package | Run and verify after stock/fork builds |
| Build environment manifest | Done for current state | `benchmarks/reports/prebuild-environment.json` records pinned revision, upstream HEAD observation, patch state, GN arg hashes, expected binary paths, non-admin shell state, Visual Studio instance metadata, ATL/MFC header/component probes, non-loopback IPv4 state for external navigation smoke, build-failure freshness metadata, and the missing ATL/MFC prerequisite; `scripts/test_atl_blocker_audit.ps1` verifies current and stale failure-log wording in the artifact audit; `scripts/test_atl_remediation_handoff.ps1` verifies the installer fails unless post-install header and component-registration checks pass |

## Optimization Completion

| Class | Status | Evidence |
| --- | --- | --- |
| Remove Chrome browser UI / minimal content-shell entrypoint | Partial | Content-shell target selected; draft viewer patch |
| Single local trusted origin | Partial | `--viewer-app-url` and navigation throttle draft |
| Disable extensions/sync/autofill/translate/background services | Partial | Runner flags cover smoke launches; source-level fork removals pending |
| GPU path, ANGLE backend, command decoder, validation, shader/pipeline warmup, compositor/presentation | Pending | No same-revision traces or fork binary yet |
| Source investigation map | Done for inspection, pending measured experiments | `docs/source_investigation.md` maps concrete Chromium files/classes for content shell, WebGL, WebGPU/Dawn, canvas/ImageBitmap, Viz, GPU process, and feature-removal work; `scripts/test_source_investigation_paths.ps1` verifies the documented revision stamp, mapped paths, and sentinel symbols against the live Chromium checkout; `scripts/test_source_investigation_revision_guard.ps1` rejects stale documented source-map revisions |
| Trace capture | Done for harness, pending official | Installed-Chrome trace smoke and summary exist for `many-draw-calls` WebGL2 | Capture same-revision stock/fork traces after binaries exist |
| Shader/resource warmup | Done for harness, pending official | Viewer supports explicit precompile/prerender warmup and v10 installed-Chrome smoke records warmup timings | Run paired same-revision stock/fork warmup experiments |
| Remove unrelated Blink/Chrome modules | Pending | Needs one-category-at-a-time build and benchmark loop |
| V8, allocator, process model choices | Pending | Needs targeted experiments behind explicit flags |
| Aggressive trusted-content flags | Partial | Draft patch defines the requested viewer-specific flags; trusted-only aliases exist for ANGLE backend, aggressive GPU bundle, relaxed WebGL validation via `--use-cmd-decoder=passthrough`, in-process GPU, and single-process; `docs/trusted_content_flags.md` documents reserved no-op gates for Blink feature disabling and direct GPU presentation; `scripts/test_viewer_patch_trusted_gates.ps1` statically verifies unsafe aliases are gated behind both `--viewer-app-url` and `--viewer-trusted-content`, while unimplemented reserved gates remain no-ops; `docs/removed_subsystems.md` lists unsafe GPU/process experiments as pending measured work | Build fork, run each experiment, and implement measured source changes for the remaining reserved gates |
| Optimization-class final audit | Partial | `audit_artifacts.ps1 -OptimizationOnly` reads `docs/optimization_log.md` and keeps all optimization classes pending until final status, measured effect, risk, evidence, and notes are present; `test_optimization_decision_audit.ps1` covers pending and synthetic-complete cases | Replace current pending decision rows with measured outcomes after official stock/fork and trusted matrix runs |
| Removed-subsystem final register audit | Partial | `audit_artifacts.ps1 -OptimizationOnly` keeps `docs/removed_subsystems.md` pending while official/trusted manifests are missing or pending-only register language remains; `scripts/test_removed_subsystems_completion_audit.ps1` covers the current incomplete state | Replace pending subsystem rows with final measured kept/reverted/blocked/not-useful decisions after official stock/fork and trusted matrix runs |

## Stability Completion

| Requirement | Status | Evidence |
| --- | --- | --- |
| 1-hour benchmark loop | Pending | `scripts/run_long_stability.ps1` exists; only short installed-Chrome validation run completed. Final audit accepts stock/fork long-stability rows only when `validate_stability_result.mjs` confirms at least 3600 measured seconds, at least 30 warmup seconds, checkout/build metadata, expected stock/fork browser executable paths, pinned Chromium revision, pin-refresh provenance, hardware GPU metadata, package-size evidence, expected viewer launch metadata, expected `instancing`/`webgl2` labels, fork revision for fork evidence, `process_rss_delta_mb <= 128`, and zero renderer resource growth |
| GPU/WebGL/WebGPU loss behavior | Partial | Installed-Chrome v11 smoke validates WebGL context loss/restoration event delivery, WebGPU device-loss signal delivery, and benchmark stability fields; `docs/stability_behavior.md` now records GPU process crash/restart as pending runtime evidence and the artifact audit keeps the stability behavior documentation row pending until validated stock/fork one-hour results exist and the document is updated with actual behavior |
| Crash detection | Partial | `run_benchmark.mjs` and `run_smoke_tests.mjs` fail when the browser exits before result capture |
| Memory leak threshold | Partial | RSS start/end/peak/delta are recorded; the project default one-hour stability gate is `process_rss_delta_mb <= 128` after warmup, enforced by `validate_stability_result.mjs` and the artifact audit | 1-hour stock/fork run pending |
| No unbounded shader/program/texture growth | Partial | v18 installed-Chrome smoke validates renderer geometry, texture, and program start/end/peak/delta fields with zero measured deltas; final audit runs `validate_stability_result.mjs --maxRendererResourceDelta 0` for the stable one-hour `instancing` WebGL2 rows | One-hour stock/fork resource growth audit still pending |

## Documentation Completion

| Document | Status |
| --- | --- |
| Architecture | Partial: `docs/architecture.md` |
| Build | Partial: `docs/build.md` |
| Benchmark methodology | Partial: `docs/benchmark_methodology.md` |
| Optimization log | Partial: `docs/optimization_log.md` |
| Removed subsystem list | Partial: `docs/removed_subsystems.md` |
| Known limitations | Partial: `docs/known_limitations.md` |
| Future work | Partial: `docs/future_work.md` |
| Rebase strategy | Partial: `docs/rebase_strategy.md` |
| Final documentation evidence audit | Partial: `scripts/test_documentation_completion_audit.ps1` keeps docs pending until official/trusted/stability evidence exists and blocker/pending language is replaced with measured outcomes; `scripts/test_performance_claim_guard.ps1` rejects unguarded stock/fork performance-win claims before official comparison evidence exists; `scripts/test_chromium_scope_guard.ps1` keeps authored README/docs/patch notes inside the Chromium-fork scope |
| Stability behavior | Partial: `docs/stability_behavior.md` |

## Current Blocking Command

The build cannot proceed until ATL/MFC is installed from an elevated PowerShell:

```powershell
.\scripts\install_vs_atl.ps1
```

That script now verifies both `atldef.h` and the `Microsoft.VisualStudio.Component.VC.ATLMFC` component registration before handing off to verification.

After that, run the full completion-oriented resume pipeline:

```powershell
.\scripts\verify_prebuild.ps1
.\scripts\run_post_atl_pipeline.ps1 `
  -RefreshChromiumPin `
  -IncludeWebGPU `
  -IncludeAggressiveGpu `
  -AggressiveAngleBackend d3d11 `
  -CaptureTrace `
  -RunTrustedExperimentMatrix `
  -TrustedMatrixInProcessGpu `
  -TrustedMatrixSingleProcess `
  -TrustedMatrixAngleBackend d3d11 `
  -TrustedMatrixReservedNoopGates `
  -RunLongStability `
  -MaxRssDeltaMb 128 `
  -MaxRendererResourceDelta 0 `
  -FinalGate
```
