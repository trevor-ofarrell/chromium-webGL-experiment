# Windows Legacy Archive

This directory contains the previous Windows/PowerShell documentation, evidence notes, and audit artifacts.

The supported repository workflow is now WSL/Linux-first from the top-level docs and `scripts/*.sh` entrypoints. Files here are preserved for historical evidence only and must not satisfy WSL final gates.
