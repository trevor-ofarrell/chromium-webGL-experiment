# Windows Legacy Scripts

These PowerShell scripts are archived from the previous Windows build and evidence harness.

The supported WSL path now lives in the Bash scripts one directory up. Do not use these scripts for new retained WSL evidence; use them only to inspect or reproduce historical Windows behavior.
